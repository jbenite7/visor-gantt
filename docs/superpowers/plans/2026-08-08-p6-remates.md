# P6 · Remates — Plan de implementación

> **Para trabajadores agénticos:** SUB-SKILL REQUERIDO: usa superpowers:subagent-driven-development (recomendado) o superpowers:executing-plans para implementar este plan tarea por tarea. Los pasos usan casillas (`- [ ]`) para seguimiento.

**Objetivo:** Cerrar P2 sin asteriscos — la Matriz y Recursos se anuncian por lo que son, el editor de matriz se puede deshacer, los encabezados abrevian en vez de cortarse, el motor aprende de correcciones hechas a mano y conserva el tramo de ejes — y dejar la documentación diciendo la verdad, con los ocho pendientes que esperan un dato o una decisión registrados donde nadie los confunda con trabajo olvidado.

**Arquitectura:** Todo lo nuevo entra como módulo puro en `v2/src/lib/` y se consume desde el componente que ya existe, para que la lógica se pruebe sin DOM: `viewSidebarBlurb.ts` alimenta `ViewSidebar`, `draftHistory.ts` da al editor de matriz una pila propia sobre su borrador local de React, `columnLabel.ts` decide entre título largo y abreviatura por ancho de columna, y `docsConsistency.ts` convierte la sincronía entre `EXPERIMENTS.md` y `DESIGN.md` en un test. Las correcciones de ubicación reutilizan `rememberCorrection`/`lookupCorrection`, que P3 ya dejó construidos y sin pantalla: solo hay que enhebrar el diccionario desde el proyecto persistido hasta `resolveTaskLocation`. Ningún bloque toca el motor de cálculo ni el esquema de base de datos: el diccionario viaja dentro del JSON `project_data` que ya se guarda.

**Stack:** Next.js 16 (App Router), TypeScript, React, Jest + Testing Library, Playwright E2E

Spec: [2026-08-08-remates-y-analiticos-design.md](../specs/2026-08-08-remates-y-analiticos-design.md), sección **P6 · Remates** (R0–R7). La sección **P5 · Analíticos avanzados** tiene plan propio y queda fuera de este.

## Restricciones globales

- **TDD estricto**: test primero, verlo fallar por el motivo esperado, luego el código mínimo. Innegociable.
- Directorio de trabajo: `v2/`. Todos los comandos se ejecutan desde ahí salvo los `git` del paso de commit, que se ejecutan desde la raíz del repositorio.
- La suite se corre **en serie**: `npx jest --runInBand`. En paralelo hay flaky conocidos y su fallo no es información.
- Punto de partida: **1.400 tests en verde sobre 143 suites**. Ninguna tarea puede dejar la suite en rojo; si una tarea rompe un test ajeno, se arregla en la misma tarea antes del commit.
- Verificación adicional antes de cerrar cada bloque: `npx eslint <archivos tocados>` y `npx tsc --noEmit 2>&1 | grep -vE "\.test\.|e2e/"` — ese filtro debe salir **vacío** (hay errores preexistentes solo en `*.test.*` y `e2e/`).
- Copy de interfaz en **español con tildes**, en lenguaje de obra, sin jerga de infraestructura.
- Identificadores, nombres de función y tipos en **inglés**, como está el resto de `v2/src`.
- Mensajes de commit en **español, sin tildes**, con el formato del repositorio: `tipo(alcance): frase en minúscula`. Alcance de este proyecto: `remates`.
- No se añaden colores nuevos: se usan los tokens de `src/app/globals.css`.
- Nada de lo que el spec declara **no implementable** (R6 salvo el renombrado, y R7 entero) se implementa: se registra por escrito y se protege con un test que verifica que el registro existe.

---

## Estructura de archivos

| Archivo | Responsabilidad | Bloque |
|---|---|---|
| `src/lib/gantt/viewHelp.ts` | Gana la entrada `observaciones`, que faltaba | R0 |
| `src/lib/gantt/viewSidebarBlurb.ts` | **Nuevo.** Qué dice cada entrada del menú según los datos que hay | R0 |
| `src/components/gantt/toolbar/ViewSidebar.tsx` | Pinta la descripción bajo la etiqueta; exporta `VIEW_TABS` para el guardián | R0 |
| `src/components/views/GanttView.tsx` | Pasa los conteos reales al menú; enhebra el diccionario de correcciones | R0, R4 |
| `src/lib/matrix/draftHistory.ts` | **Nuevo.** Pila de deshacer/rehacer del borrador, pura y genérica | R1 |
| `src/lib/matrix/useDraftHistory.ts` | **Nuevo.** El hook de React que envuelve la pila | R1 |
| `src/components/views/MatrixEditorView.tsx` | Todas las mutaciones del borrador pasan por la pila; `Cmd+Z` / `Cmd+Shift+Z` | R1 |
| `src/lib/gantt/columnLabel.ts` | **Nuevo.** Elige entre título completo y abreviatura según el ancho | R2 |
| `src/components/gantt/table/ColumnSelector.tsx` | `ColumnConfig` gana `shortLabelEs` / `shortLabelEn` | R2 |
| `src/components/gantt/table/GanttTable.tsx` | Las 14 columnas declaran su forma corta; el encabezado la elige por ancho | R2 |
| `src/components/gantt/table/ColumnHeader.tsx` | Recibe la forma corta y conserva el nombre completo en `title` | R2 |
| `src/lib/docs/docsConsistency.ts` | **Nuevo.** Lee los documentos y cruza hallazgos con experimentos | R3, R6, R7 |
| `docs/EXPERIMENTS.md` | Se corrigen las dos afirmaciones falsas; se registran R6 y R7 | R3, R6, R7 |
| `docs/DESIGN.md` | Los hallazgos ya cerrados dejan de figurar como `open` | R3 |
| `docs/PRODUCT.md` | C3 revertido con motivo; Outcome Roadmap al día | R3 |
| `src/app/actions/project.ts` | `ProjectData` gana `detectionDictionary`, que se serializa y se lee | R4 |
| `src/lib/scheduling/lob.ts` | La Línea de Balance resuelve ubicaciones con el diccionario | R4 |
| `src/components/lob/LocationCorrectionPanel.tsx` | **Nuevo.** Corregir a mano la ubicación de una tarea | R4 |
| `src/lib/scheduling/detection/location.ts` | El módulo conserva el tramo de ejes que hoy descarta | R5 |
| `src/components/views/TypicalUnitView.tsx` | El comentario deja de nombrar la palabra prohibida | R6 |

---

# BLOQUE R0 — La Matriz y Recursos se anuncian por lo que son

Va primero porque es la mejor relación entre esfuerzo y resultado de todo el proyecto: no construye nada nuevo, solo enseña lo que ya está.

## Tarea 1: Qué dice cada entrada del menú

**Archivos:**
- Crear: `src/lib/gantt/viewSidebarBlurb.ts`
- Crear: `src/lib/gantt/viewSidebarBlurb.test.ts`
- Modificar: `src/lib/gantt/viewHelp.ts:11-98` (añadir la entrada `observaciones`, que hoy no existe en `VIEW_HELP`)

**Interfaces:**
- Consume: `viewHelpFor(view: ViewType): ViewHelp | null` de `src/lib/gantt/viewHelp.ts`; `ViewType` de `src/components/gantt/toolbar/viewTypes.ts`.
- Produce:
  - `export interface ViewSidebarContext { areaCount?: number; resourceCount?: number }`
  - `export function viewSidebarBlurb(view: ViewType, context?: ViewSidebarContext): string`

- [ ] **Paso 1: Escribir el test que falla**

```ts
import { viewSidebarBlurb } from "./viewSidebarBlurb";

describe("viewSidebarBlurb (R0: la entrada del menú dice qué hay dentro)", () => {
  test("con matriz, la Matriz dice cuántas ubicaciones tiene", () => {
    expect(viewSidebarBlurb("matrix", { areaCount: 26 })).toBe(
      "26 ubicaciones programadas",
    );
  });

  test("con una sola ubicación no dice «1 ubicaciones»", () => {
    expect(viewSidebarBlurb("matrix", { areaCount: 1 })).toBe(
      "1 ubicación programada",
    );
  });

  test("sin matriz, explica para qué sirve crearla", () => {
    expect(viewSidebarBlurb("matrix", { areaCount: 0 })).toBe(
      "Todavía no hay matriz: cruza alcances con ubicaciones para armar la obra por celdas.",
    );
  });

  test("con recursos, Recursos dice cuántos hay", () => {
    expect(viewSidebarBlurb("resources", { resourceCount: 12 })).toBe(
      "12 recursos asignados",
    );
  });

  test("sin recursos, explica para qué sirve la vista", () => {
    expect(viewSidebarBlurb("resources", { resourceCount: 0 })).toBe(
      "Todavía no hay recursos: aquí se ve quién y qué hace falta en cada actividad.",
    );
  });

  test("el resto de vistas reutiliza el propósito ya escrito en la ayuda por vista", () => {
    expect(viewSidebarBlurb("scurve")).toBe(
      "Cómo se acumula el avance en el tiempo, para comparar lo planeado con lo real.",
    );
  });

  test("Observaciones tiene propósito escrito, no cadena vacía", () => {
    expect(viewSidebarBlurb("observaciones").length).toBeGreaterThan(0);
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/gantt/viewSidebarBlurb.test.ts`
Esperado: FALLA con `Cannot find module './viewSidebarBlurb' from 'src/lib/gantt/viewSidebarBlurb.test.ts'`

- [ ] **Paso 3: Implementación mínima**

Primero, la entrada que falta en `src/lib/gantt/viewHelp.ts`. Se inserta dentro del objeto `VIEW_HELP`, justo después de la entrada `matrix` (línea 92-98) y antes del cierre `};`:

```ts
  observaciones: {
    title: "Observaciones",
    purpose:
      "Lo que se reporta en obra y el compromiso semanal: qué está frenando, quién responde y para cuándo.",
    needs: "Nada: se puede abrir una observación sobre cualquier tarea desde el primer día.",
  },
```

Después, el módulo nuevo `src/lib/gantt/viewSidebarBlurb.ts`:

```ts
import type { ViewType } from "@/components/gantt/toolbar/viewTypes";
import { viewHelpFor } from "./viewHelp";

/**
 * Lo que la entrada del menú dice de sí misma.
 *
 * «Matriz» era un botón de 12 caracteres con 26 tareas de trabajo detrás: la
 * puerta no delataba la habitación. El texto no se redacta aquí de cero — el
 * propósito de cada vista ya estaba escrito en la ayuda por vista (E8) — pero
 * las dos vistas que dependen de que haya datos cargados dicen además cuánto
 * hay, porque «26 ubicaciones» convence de entrar y «Matriz» no.
 */
export interface ViewSidebarContext {
  /** Ubicaciones del plan matricial, si lo hay. */
  areaCount?: number;
  /** Recursos cargados en el proyecto. */
  resourceCount?: number;
}

export function viewSidebarBlurb(
  view: ViewType,
  context: ViewSidebarContext = {},
): string {
  if (view === "matrix") {
    const areas = context.areaCount ?? 0;
    if (areas <= 0) {
      return "Todavía no hay matriz: cruza alcances con ubicaciones para armar la obra por celdas.";
    }
    return areas === 1
      ? "1 ubicación programada"
      : `${areas} ubicaciones programadas`;
  }

  if (view === "resources") {
    const resources = context.resourceCount ?? 0;
    if (resources <= 0) {
      return "Todavía no hay recursos: aquí se ve quién y qué hace falta en cada actividad.";
    }
    return resources === 1 ? "1 recurso asignado" : `${resources} recursos asignados`;
  }

  return viewHelpFor(view)?.purpose ?? "";
}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/gantt/viewSidebarBlurb.test.ts src/components/gantt/ViewHelpPanel.test.tsx --runInBand`
Esperado: PASA — las dos suites en verde (la segunda confirma que añadir `observaciones` a `VIEW_HELP` no rompió el panel de ayuda).

- [ ] **Paso 5: Commit**

```bash
git add v2/src/lib/gantt/viewSidebarBlurb.ts v2/src/lib/gantt/viewSidebarBlurb.test.ts v2/src/lib/gantt/viewHelp.ts && git commit -m "feat(remates): cada entrada del menu sabe decir que hay dentro"
```

---

## Tarea 2: El menú pinta la descripción

**Archivos:**
- Modificar: `src/components/gantt/toolbar/ViewSidebar.tsx:20-114` (interfaz `ViewTab`, `VIEW_TABS`, `ViewSidebarProps` y el `<button>` del `map`)
- Modificar: `src/app/globals.css` (añadir `.project-view-sidebar__blurb` al final del bloque de la barra de vistas)
- Test: `src/components/gantt/toolbar/ViewSidebar.test.tsx`

**Interfaces:**
- Consume: `viewSidebarBlurb(view: ViewType, context?: ViewSidebarContext): string` y `ViewSidebarContext` de `src/lib/gantt/viewSidebarBlurb.ts`.
- Produce:
  - `export const VIEW_TABS: ViewTab[]` (deja de ser privado; lo necesita el guardián de la tarea 3)
  - `export interface ViewTab { id: ViewType; labelEs: string; labelEn: string; icon: React.ComponentType<{ size?: number; className?: string }>; group: ViewGroupId }`
  - `ViewSidebarProps` gana `blurbContext?: ViewSidebarContext`
  - Cada entrada expone `data-testid="sidebar-blurb-<id>"`

- [ ] **Paso 1: Escribir el test que falla**

Se añade al final de `src/components/gantt/toolbar/ViewSidebar.test.tsx`, dentro del archivo existente:

```tsx
describe("ViewSidebar · descripción por entrada (R0)", () => {
  test("la Matriz anuncia cuántas ubicaciones hay detrás", () => {
    render(
      <ViewSidebar
        activeView="gantt"
        onViewChange={jest.fn()}
        blurbContext={{ areaCount: 26, resourceCount: 0 }}
      />,
    );

    expect(screen.getByTestId("sidebar-blurb-matrix")).toHaveTextContent(
      "26 ubicaciones programadas",
    );
  });

  test("sin recursos, Recursos explica para qué sirve en vez de quedarse mudo", () => {
    render(
      <ViewSidebar
        activeView="gantt"
        onViewChange={jest.fn()}
        blurbContext={{ areaCount: 0, resourceCount: 0 }}
      />,
    );

    expect(screen.getByTestId("sidebar-blurb-resources")).toHaveTextContent(
      "Todavía no hay recursos",
    );
  });

  test("el nombre completo sigue siendo el rótulo accesible del botón", () => {
    render(<ViewSidebar activeView="gantt" onViewChange={jest.fn()} />);

    expect(screen.getByTestId("sidebar-view-matrix")).toHaveAttribute(
      "aria-label",
      "Matriz",
    );
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/gantt/toolbar/ViewSidebar.test.tsx -t "anuncia cuántas ubicaciones"`
Esperado: FALLA con `Unable to find an element by: [data-testid="sidebar-blurb-matrix"]`

- [ ] **Paso 3: Implementación mínima**

En `src/components/gantt/toolbar/ViewSidebar.tsx`, el import nuevo junto a los que ya hay:

```tsx
import { viewSidebarBlurb, type ViewSidebarContext } from "@/lib/gantt/viewSidebarBlurb";
```

`ViewTab` y `VIEW_TABS` pasan a exportarse (solo cambia la palabra `export` al principio de cada declaración):

```tsx
export interface ViewTab {
  id: ViewType;
  labelEs: string;
  labelEn: string;
  icon: React.ComponentType<{ size?: number; className?: string }>;
  group: ViewGroupId;
}
```

```tsx
export const VIEW_TABS: ViewTab[] = [
```

Las props ganan el contexto:

```tsx
interface ViewSidebarProps {
  activeView: ViewType;
  onViewChange: (view: ViewType) => void;
  locale?: UILocale;
  /** Conteos reales del proyecto: sin ellos la entrada explica para qué sirve. */
  blurbContext?: ViewSidebarContext;
}

export default function ViewSidebar({
  activeView,
  onViewChange,
  locale = "es",
  blurbContext,
}: ViewSidebarProps) {
```

Y dentro del `map`, el `<button>` pasa a llevar la descripción bajo la etiqueta:

```tsx
            const isActive = activeView === tab.id;
            const Icon = tab.icon;
            const label = locale === "en" ? tab.labelEn : tab.labelEs;
            const blurb = viewSidebarBlurb(tab.id, blurbContext);

            return (
              <button
                key={tab.id}
                data-testid={`sidebar-view-${tab.id}`}
                role="tab"
                aria-selected={isActive}
                aria-label={label}
                onClick={() => onViewChange(tab.id)}
                title={blurb ? `${label} — ${blurb}` : label}
                className="project-view-sidebar__item"
                type="button"
              >
                <Icon className="project-view-sidebar__icon" aria-hidden />
                <span className="project-view-sidebar__label">{label}</span>
                {blurb && (
                  <span
                    data-testid={`sidebar-blurb-${tab.id}`}
                    className="project-view-sidebar__blurb"
                  >
                    {blurb}
                  </span>
                )}
              </button>
            );
```

En `src/app/globals.css`, inmediatamente después de la regla `.project-view-sidebar__label { ... }` que ya existe:

```css
.project-view-sidebar__blurb {
  display: block;
  color: var(--color-text-muted);
  font-size: 0.625rem;
  line-height: 1.25;
  text-align: center;
  text-wrap: balance;
}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/gantt/toolbar/ViewSidebar.test.tsx --runInBand`
Esperado: PASA — la suite entera, incluidos los tests previos que comprueban el orden de las once entradas.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/gantt/toolbar/ViewSidebar.tsx v2/src/components/gantt/toolbar/ViewSidebar.test.tsx v2/src/app/globals.css && git commit -m "feat(remates): la puerta deja de ser una etiqueta desnuda"
```

---

## Tarea 3: El guardián — ninguna entrada vuelve a ser solo una etiqueta

**Archivos:**
- Crear: `src/components/gantt/toolbar/viewSidebarCopy.test.ts`

**Interfaces:**
- Consume: `VIEW_TABS: ViewTab[]` de `src/components/gantt/toolbar/ViewSidebar.tsx`; `viewSidebarBlurb(view, context?)` de `src/lib/gantt/viewSidebarBlurb.ts`.
- Produce: nada; es un test guardián sin módulo asociado.

- [ ] **Paso 1: Escribir el test que falla**

```ts
import { VIEW_TABS } from "./ViewSidebar";
import { viewSidebarBlurb } from "@/lib/gantt/viewSidebarBlurb";

/**
 * El reverso de la valla: R0 arregla las entradas de hoy, este test impide
 * que la número doce entre muda. Es el mismo patrón del detector de copy.
 */
describe("Detector de entradas mudas en el menú (R0)", () => {
  test("toda entrada del menú tiene descripción, con datos y sin ellos", () => {
    const mudasSinDatos = VIEW_TABS.filter(
      (tab) => viewSidebarBlurb(tab.id, { areaCount: 0, resourceCount: 0 }).trim() === "",
    ).map((tab) => tab.id);

    const mudasConDatos = VIEW_TABS.filter(
      (tab) => viewSidebarBlurb(tab.id, { areaCount: 5, resourceCount: 5 }).trim() === "",
    ).map((tab) => tab.id);

    expect({ mudasSinDatos, mudasConDatos }).toEqual({
      mudasSinDatos: [],
      mudasConDatos: [],
    });
  });

  test("ninguna descripción repite literalmente su etiqueta", () => {
    const redundantes = VIEW_TABS.filter(
      (tab) => viewSidebarBlurb(tab.id, { areaCount: 5, resourceCount: 5 }) === tab.labelEs,
    ).map((tab) => tab.id);

    expect(redundantes).toEqual([]);
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Ejecutar el test **antes** de haber añadido `observaciones` a `VIEW_HELP` no es posible aquí porque la tarea 1 ya lo añadió; para verlo fallar por el motivo esperado, comentar temporalmente la entrada `observaciones` en `src/lib/gantt/viewHelp.ts` y correr:

Comando: `cd v2 && npx jest src/components/gantt/toolbar/viewSidebarCopy.test.ts -t "toda entrada del menú"`
Esperado: FALLA con `mudasSinDatos: ["observaciones"]` frente al `[]` esperado. Descomentar la entrada acto seguido.

- [ ] **Paso 3: Implementación mínima**

No hay código de producción nuevo: la implementación que el test exige es la entrada `observaciones` de `VIEW_HELP` que la tarea 1 ya escribió. Se restaura tal cual, sin el comentario del paso 2:

```ts
  observaciones: {
    title: "Observaciones",
    purpose:
      "Lo que se reporta en obra y el compromiso semanal: qué está frenando, quién responde y para cuándo.",
    needs: "Nada: se puede abrir una observación sobre cualquier tarea desde el primer día.",
  },
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/gantt/toolbar --runInBand`
Esperado: PASA — las tres suites de `toolbar/` en verde.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/gantt/toolbar/viewSidebarCopy.test.ts v2/src/lib/gantt/viewHelp.ts && git commit -m "test(remates): la entrada numero doce no podra entrar muda"
```

---

## Tarea 4: El menú recibe los conteos reales del proyecto

**Archivos:**
- Modificar: `src/components/views/GanttView.tsx` (el `<ViewSidebar ... />` que renderiza la barra lateral)
- Test: `src/components/views/GanttView.test.tsx`

**Interfaces:**
- Consume: `ViewSidebarProps.blurbContext?: ViewSidebarContext` de la tarea 2.
- Produce: nada nuevo; es cableado.

- [ ] **Paso 1: Escribir el test que falla**

Se añade al final de `src/components/views/GanttView.test.tsx`, reutilizando los helpers de render que ya tiene el archivo:

```tsx
describe("GanttView · el menú refleja el proyecto real (R0)", () => {
  test("la Matriz del menú cuenta las ubicaciones del plan cargado", () => {
    renderGanttView();

    expect(screen.getByTestId("sidebar-blurb-matrix")).toHaveTextContent(
      /ubicaci(ón|ones)/,
    );
  });
});
```

> Nota para quien ejecute: `renderGanttView` es el helper que ya usa esta suite. Si el helper por defecto no monta un `matrixPlan`, se le pasa el mismo fixture de matriz que usan los tests de `handleApplyMatrixPlan` en este archivo (búscalo por `matrixPlan:` dentro de la suite). El aserto sólo exige que el texto hable de ubicaciones, con matriz o sin ella.

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/views/GanttView.test.tsx -t "cuenta las ubicaciones del plan cargado" --runInBand`
Esperado: FALLA con `Unable to find an element by: [data-testid="sidebar-blurb-matrix"]` — la barra se monta sin `blurbContext` y `viewSidebarBlurb("matrix")` devuelve el texto de «todavía no hay matriz», pero el nodo no llega a existir hasta que se pasa el contexto… si el nodo ya existiera por el texto por defecto, el fallo será el aserto de contenido, que es igual de válido como motivo esperado.

- [ ] **Paso 3: Implementación mínima**

En `src/components/views/GanttView.tsx`, junto al resto de `useMemo` que ya derivan datos del proyecto (bloque de las líneas 530-580, donde vive `automaticLOB`):

```tsx
  const sidebarBlurbContext = useMemo(
    () => ({
      areaCount: syncedMatrixPlan?.areas.length ?? matrixPlan?.areas.length ?? 0,
      resourceCount: calculatedResources.length,
    }),
    [calculatedResources.length, matrixPlan, syncedMatrixPlan],
  );
```

Y en el punto donde se renderiza la barra, se le pasa:

```tsx
              <ViewSidebar
                activeView={activeView}
                onViewChange={setActiveView}
                locale={effectiveLocale}
                blurbContext={sidebarBlurbContext}
              />
```

> El `locale` solo se mantiene si el `<ViewSidebar>` actual ya lo pasaba; si no lo pasa, se deja como está y solo se añade la línea `blurbContext`.

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/views/GanttView.test.tsx --runInBand`
Esperado: PASA — la suite completa de `GanttView`.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/views/GanttView.tsx v2/src/components/views/GanttView.test.tsx && git commit -m "feat(remates): el menu cuenta las ubicaciones y los recursos que hay de verdad"
```

---

# BLOQUE R1 — Deshacer en el editor de matriz

La única acción destructiva de la app sin vuelta atrás. La pila es **local al borrador**, no al proyecto: `runUndoable` opera sobre `ProjectContext` y el borrador es estado de React (`MatrixEditorView.tsx:330`).

## Tarea 5: La pila del borrador, pura y sin React

**Archivos:**
- Crear: `src/lib/matrix/draftHistory.ts`
- Crear: `src/lib/matrix/draftHistory.test.ts`

**Interfaces:**
- Consume: nada.
- Produce:
  - `export const DRAFT_HISTORY_LIMIT = 50`
  - `export interface DraftHistory<T> { past: T[]; present: T | undefined; future: T[] }`
  - `export function createDraftHistory<T>(present: T | undefined): DraftHistory<T>`
  - `export function commitDraftState<T>(history: DraftHistory<T>, next: T): DraftHistory<T>`
  - `export function undoDraftState<T>(history: DraftHistory<T>): DraftHistory<T>`
  - `export function redoDraftState<T>(history: DraftHistory<T>): DraftHistory<T>`
  - `export function canUndoDraft<T>(history: DraftHistory<T>): boolean`
  - `export function canRedoDraft<T>(history: DraftHistory<T>): boolean`

- [ ] **Paso 1: Escribir el test que falla**

```ts
import {
  DRAFT_HISTORY_LIMIT,
  canRedoDraft,
  canUndoDraft,
  commitDraftState,
  createDraftHistory,
  redoDraftState,
  undoDraftState,
} from "./draftHistory";

describe("draftHistory (R1: la pila propia del editor de matriz)", () => {
  test("un borrador recién abierto no tiene nada que deshacer", () => {
    const history = createDraftHistory({ n: 0 });

    expect(history.present).toEqual({ n: 0 });
    expect(canUndoDraft(history)).toBe(false);
    expect(canRedoDraft(history)).toBe(false);
  });

  test("deshacer devuelve exactamente el estado anterior", () => {
    let history = createDraftHistory({ n: 0 });
    history = commitDraftState(history, { n: 1 });
    history = commitDraftState(history, { n: 2 });

    history = undoDraftState(history);

    expect(history.present).toEqual({ n: 1 });
    expect(canUndoDraft(history)).toBe(true);
    expect(canRedoDraft(history)).toBe(true);
  });

  test("rehacer devuelve lo deshecho", () => {
    let history = createDraftHistory({ n: 0 });
    history = commitDraftState(history, { n: 1 });
    history = undoDraftState(history);
    history = redoDraftState(history);

    expect(history.present).toEqual({ n: 1 });
    expect(canRedoDraft(history)).toBe(false);
  });

  test("un cambio nuevo después de deshacer descarta el futuro", () => {
    let history = createDraftHistory({ n: 0 });
    history = commitDraftState(history, { n: 1 });
    history = undoDraftState(history);
    history = commitDraftState(history, { n: 9 });

    expect(history.present).toEqual({ n: 9 });
    expect(canRedoDraft(history)).toBe(false);
  });

  test("deshacer sin pasado no rompe ni cambia nada", () => {
    const history = createDraftHistory({ n: 0 });

    expect(undoDraftState(history)).toEqual(history);
    expect(redoDraftState(history)).toEqual(history);
  });

  test("el pasado se corta en el tope y conserva los más recientes", () => {
    let history = createDraftHistory({ n: 0 });
    for (let i = 1; i <= DRAFT_HISTORY_LIMIT + 10; i += 1) {
      history = commitDraftState(history, { n: i });
    }

    expect(history.past).toHaveLength(DRAFT_HISTORY_LIMIT);
    expect(history.past[history.past.length - 1]).toEqual({
      n: DRAFT_HISTORY_LIMIT + 9,
    });
  });

  test("sin borrador todavía, el primer cambio no inventa un pasado", () => {
    const history = commitDraftState(createDraftHistory<{ n: number }>(undefined), {
      n: 1,
    });

    expect(history.past).toEqual([]);
    expect(canUndoDraft(history)).toBe(false);
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/matrix/draftHistory.test.ts`
Esperado: FALLA con `Cannot find module './draftHistory' from 'src/lib/matrix/draftHistory.test.ts'`

- [ ] **Paso 3: Implementación mínima**

```ts
/**
 * El historial del borrador del editor de matriz.
 *
 * No es `runUndoable`: aquello opera sobre el proyecto persistido en
 * `ProjectContext`, y el borrador de la matriz es estado local de React que
 * todavía no forma parte del proyecto. Mezclarlos llenaría el historial
 * general de estados que nunca se guardaron.
 *
 * Se descarta al salir sin guardar, porque el borrador entero se descarta con
 * él. Al guardar, el conjunto entra al historial general como **un solo
 * cambio**, no como N pasos intermedios.
 *
 * Genérico a propósito: aquí no se sabe qué es un `MatrixPlan`, y así se
 * prueba entero sin construir uno.
 */
export const DRAFT_HISTORY_LIMIT = 50;

export interface DraftHistory<T> {
  past: T[];
  present: T | undefined;
  future: T[];
}

export function createDraftHistory<T>(present: T | undefined): DraftHistory<T> {
  return { past: [], present, future: [] };
}

export function commitDraftState<T>(
  history: DraftHistory<T>,
  next: T,
): DraftHistory<T> {
  // Sin borrador previo no hay nada que deshacer: crear el primer plan no es
  // un cambio sobre otro estado, es el punto de partida.
  if (history.present === undefined) {
    return { past: [], present: next, future: [] };
  }

  const past = [...history.past, history.present];
  return {
    past: past.length > DRAFT_HISTORY_LIMIT ? past.slice(past.length - DRAFT_HISTORY_LIMIT) : past,
    present: next,
    future: [],
  };
}

export function undoDraftState<T>(history: DraftHistory<T>): DraftHistory<T> {
  if (!canUndoDraft(history) || history.present === undefined) return history;
  const previous = history.past[history.past.length - 1];
  return {
    past: history.past.slice(0, -1),
    present: previous,
    future: [history.present, ...history.future],
  };
}

export function redoDraftState<T>(history: DraftHistory<T>): DraftHistory<T> {
  if (!canRedoDraft(history) || history.present === undefined) return history;
  const [next, ...rest] = history.future;
  return {
    past: [...history.past, history.present],
    present: next,
    future: rest,
  };
}

export function canUndoDraft<T>(history: DraftHistory<T>): boolean {
  return history.past.length > 0;
}

export function canRedoDraft<T>(history: DraftHistory<T>): boolean {
  return history.future.length > 0;
}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/matrix/draftHistory.test.ts`
Esperado: PASA — 7 tests en verde.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/lib/matrix/draftHistory.ts v2/src/lib/matrix/draftHistory.test.ts && git commit -m "feat(remates): el borrador de la matriz ya tiene pila propia"
```

---

## Tarea 6: El hook que el editor va a usar

**Archivos:**
- Crear: `src/lib/matrix/useDraftHistory.ts`
- Crear: `src/lib/matrix/useDraftHistory.test.tsx`

**Interfaces:**
- Consume: `createDraftHistory`, `commitDraftState`, `undoDraftState`, `redoDraftState`, `canUndoDraft`, `canRedoDraft`, `DraftHistory<T>` de `src/lib/matrix/draftHistory.ts`.
- Produce:
  - `export interface DraftHistoryHandle<T> { draft: T | undefined; commitDraft: (next: T | ((current: T | undefined) => T | undefined)) => void; resetDraft: (next: T | undefined) => void; undoDraft: () => void; redoDraft: () => void; canUndo: boolean; canRedo: boolean }`
  - `export function useDraftHistory<T>(initial: T | undefined): DraftHistoryHandle<T>`

- [ ] **Paso 1: Escribir el test que falla**

```tsx
/**
 * @jest-environment jsdom
 */

import "@testing-library/jest-dom";
import { act, render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { useDraftHistory } from "./useDraftHistory";

function Probe() {
  const { draft, commitDraft, resetDraft, undoDraft, redoDraft, canUndo, canRedo } =
    useDraftHistory<{ n: number }>({ n: 0 });

  return (
    <div>
      <span data-testid="valor">{draft ? draft.n : "sin borrador"}</span>
      <span data-testid="puede-deshacer">{String(canUndo)}</span>
      <span data-testid="puede-rehacer">{String(canRedo)}</span>
      <button type="button" onClick={() => commitDraft({ n: (draft?.n ?? 0) + 1 })}>
        sumar
      </button>
      <button
        type="button"
        onClick={() => commitDraft((current) => (current ? { n: current.n * 10 } : current))}
      >
        por diez
      </button>
      <button type="button" onClick={undoDraft}>deshacer</button>
      <button type="button" onClick={redoDraft}>rehacer</button>
      <button type="button" onClick={() => resetDraft({ n: 100 })}>recargar</button>
    </div>
  );
}

describe("useDraftHistory (R1)", () => {
  test("deshacer devuelve el borrador al estado anterior", async () => {
    const user = userEvent.setup();
    render(<Probe />);

    await user.click(screen.getByRole("button", { name: "sumar" }));
    await user.click(screen.getByRole("button", { name: "sumar" }));
    expect(screen.getByTestId("valor")).toHaveTextContent("2");

    await user.click(screen.getByRole("button", { name: "deshacer" }));
    expect(screen.getByTestId("valor")).toHaveTextContent("1");
    expect(screen.getByTestId("puede-rehacer")).toHaveTextContent("true");

    await user.click(screen.getByRole("button", { name: "rehacer" }));
    expect(screen.getByTestId("valor")).toHaveTextContent("2");
  });

  test("la forma con función recibe el borrador actual", async () => {
    const user = userEvent.setup();
    render(<Probe />);

    await user.click(screen.getByRole("button", { name: "sumar" }));
    await user.click(screen.getByRole("button", { name: "por diez" }));

    expect(screen.getByTestId("valor")).toHaveTextContent("10");
    await user.click(screen.getByRole("button", { name: "deshacer" }));
    expect(screen.getByTestId("valor")).toHaveTextContent("1");
  });

  test("recargar el borrador tira la pila: lo deshecho ya no existe", async () => {
    const user = userEvent.setup();
    render(<Probe />);

    await user.click(screen.getByRole("button", { name: "sumar" }));
    await user.click(screen.getByRole("button", { name: "recargar" }));

    expect(screen.getByTestId("valor")).toHaveTextContent("100");
    expect(screen.getByTestId("puede-deshacer")).toHaveTextContent("false");
    expect(screen.getByTestId("puede-rehacer")).toHaveTextContent("false");
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/matrix/useDraftHistory.test.tsx`
Esperado: FALLA con `Cannot find module './useDraftHistory' from 'src/lib/matrix/useDraftHistory.test.tsx'`

- [ ] **Paso 3: Implementación mínima**

```ts
"use client";

import { useCallback, useMemo, useState } from "react";
import {
  canRedoDraft,
  canUndoDraft,
  commitDraftState,
  createDraftHistory,
  redoDraftState,
  undoDraftState,
  type DraftHistory,
} from "./draftHistory";

/**
 * La pila del borrador vestida de `useState`.
 *
 * `commitDraft` sustituye a `setDraft`: acepta el valor nuevo o la forma con
 * función, igual que `setState`, para que el editor no tenga que cambiar de
 * estilo en cada llamada. `resetDraft` es la puerta de atrás: recargar el
 * borrador desde el proyecto o descartarlo **tira la pila entera**, porque la
 * pila describe un borrador que ya no existe.
 */
export interface DraftHistoryHandle<T> {
  draft: T | undefined;
  commitDraft: (next: T | ((current: T | undefined) => T | undefined)) => void;
  resetDraft: (next: T | undefined) => void;
  undoDraft: () => void;
  redoDraft: () => void;
  canUndo: boolean;
  canRedo: boolean;
}

export function useDraftHistory<T>(initial: T | undefined): DraftHistoryHandle<T> {
  const [history, setHistory] = useState<DraftHistory<T>>(() =>
    createDraftHistory(initial),
  );

  const commitDraft = useCallback(
    (next: T | ((current: T | undefined) => T | undefined)) => {
      setHistory((current) => {
        const value =
          typeof next === "function"
            ? (next as (state: T | undefined) => T | undefined)(current.present)
            : next;
        if (value === undefined || value === current.present) return current;
        return commitDraftState(current, value);
      });
    },
    [],
  );

  const resetDraft = useCallback((next: T | undefined) => {
    setHistory(createDraftHistory(next));
  }, []);

  const undoDraft = useCallback(() => setHistory((current) => undoDraftState(current)), []);
  const redoDraft = useCallback(() => setHistory((current) => redoDraftState(current)), []);

  return useMemo(
    () => ({
      draft: history.present,
      commitDraft,
      resetDraft,
      undoDraft,
      redoDraft,
      canUndo: canUndoDraft(history),
      canRedo: canRedoDraft(history),
    }),
    [commitDraft, history, redoDraft, resetDraft, undoDraft],
  );
}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/matrix/useDraftHistory.test.tsx`
Esperado: PASA — 3 tests en verde.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/lib/matrix/useDraftHistory.ts v2/src/lib/matrix/useDraftHistory.test.tsx && git commit -m "feat(remates): el hook que le da deshacer al borrador"
```

---

## Tarea 7: Todas las mutaciones del borrador pasan por la pila

El spec es explícito: **toda** operación que hoy llame a `applyNextDraft` pasa por la pila, no solo los dos borrados. El inventario real de `MatrixEditorView.tsx`, leído en esta rama, es:

| Línea | Operación |
|---|---|
| 355 | `descartarCambios` — recarga el borrador desde el proyecto |
| 501 | `applyToSelection` — edición masiva de celdas (forma con función) |
| 576 | `createDraft` — crear el plan por defecto |
| 601 | `updateCell` |
| 637-639 | `applyNextDraft` (la propia función) |
| 660 | `activateAllCells` |
| 697 | `addScope` |
| 714 | `addArea` |
| 751 | `addScopeChild` |
| 763 | `addScopeSibling` |
| 798 | `addAreaChild` |
| 809 | `addAreaSibling` |
| 826 | `deleteScope` |
| 852 | `deleteArea` |
| 857 | `updateScopeDetails` |
| 865 | `updateAreaDetails` |
| 875 | `updateScopeRecipe` |
| 896 | `duplicateLocation` |
| 906 | `createLocationRange` |
| 911 | `replaceRecipe` |
| 920 | `pickTemplate` |
| 948 | `acceptProposal` |
| 953 | `approveFeedback` |
| 958 | `dismissFeedback` |
| 1313, 1322 | nombre y fecha de inicio del plan |

**Archivos:**
- Modificar: `src/components/views/MatrixEditorView.tsx:330-332` (el `useState` del borrador), y cada una de las líneas del inventario
- Test: `src/components/views/MatrixEditorView.test.tsx`

**Interfaces:**
- Consume: `useDraftHistory<T>(initial: T | undefined): DraftHistoryHandle<T>` de `src/lib/matrix/useDraftHistory.ts`.
- Produce: nada exportado; dentro del componente, `draft`, `commitDraft`, `resetDraft`, `undoDraft`, `redoDraft`, `canUndo`, `canRedo` sustituyen a `draft` / `setDraft`.

- [ ] **Paso 1: Escribir el test que falla**

Se añade al final de `src/components/views/MatrixEditorView.test.tsx`, con los helpers de render y fixtures que la suite ya tiene:

```tsx
describe("MatrixEditorView · deshacer dentro del editor (R1)", () => {
  test("borrar un alcance y deshacer devuelve el alcance y sus celdas", async () => {
    const user = userEvent.setup();
    jest.spyOn(window, "confirm").mockReturnValue(true);
    renderMatrixEditor();

    const alcancesAntes = screen.getAllByTestId(/^matrix-scope-row-/).length;
    const celdasAntes = screen.getAllByTestId(/^matrix-cell-/).length;

    await user.click(screen.getAllByRole("button", { name: /eliminar alcance/i })[0]);
    expect(screen.getAllByTestId(/^matrix-scope-row-/).length).toBe(alcancesAntes - 1);

    await user.keyboard("{Meta>}z{/Meta}");

    expect(screen.getAllByTestId(/^matrix-scope-row-/).length).toBe(alcancesAntes);
    expect(screen.getAllByTestId(/^matrix-cell-/).length).toBe(celdasAntes);
  });

  test("rehacer vuelve a aplicar lo deshecho", async () => {
    const user = userEvent.setup();
    jest.spyOn(window, "confirm").mockReturnValue(true);
    renderMatrixEditor();

    const alcancesAntes = screen.getAllByTestId(/^matrix-scope-row-/).length;

    await user.click(screen.getAllByRole("button", { name: /eliminar alcance/i })[0]);
    await user.keyboard("{Meta>}z{/Meta}");
    await user.keyboard("{Meta>}{Shift>}z{/Shift}{/Meta}");

    expect(screen.getAllByTestId(/^matrix-scope-row-/).length).toBe(alcancesAntes - 1);
  });

  test("N operaciones seguidas de un guardado producen UNA entrada en el historial general", async () => {
    const user = userEvent.setup();
    const onApplyMatrixPlan = jest.fn();
    renderMatrixEditor({ onApplyMatrixPlan });

    await user.type(screen.getByLabelText(/nuevo alcance/i), "Estructura");
    await user.click(screen.getByRole("button", { name: /agregar alcance/i }));
    await user.type(screen.getByLabelText(/nueva ubicación/i), "Piso 4");
    await user.click(screen.getByRole("button", { name: /agregar ubicación/i }));

    await user.click(screen.getByRole("button", { name: /aplicar/i }));

    expect(onApplyMatrixPlan).toHaveBeenCalledTimes(1);
  });
});
```

> Nota para quien ejecute: los `data-testid` y los rótulos accesibles (`eliminar alcance`, `nuevo alcance`, `agregar alcance`, `nueva ubicación`, `agregar ubicación`) se toman **tal cual estén** en `MatrixEditorView.tsx` y en los tests que ya existen en esta misma suite. Léelos antes de escribir el test y usa los reales; no inventes `testid` nuevos ni los añadas al componente para este test.

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/views/MatrixEditorView.test.tsx -t "deshacer devuelve el alcance" --runInBand`
Esperado: FALLA — tras `{Meta>}z{/Meta}` el número de filas de alcance sigue siendo `alcancesAntes - 1`: `expect(received).toBe(expected) // Expected: 4, Received: 3`. El editor no escucha `Cmd+Z`.

- [ ] **Paso 3: Implementación mínima**

En `src/components/views/MatrixEditorView.tsx`, el import nuevo:

```tsx
import { useDraftHistory } from "@/lib/matrix/useDraftHistory";
```

El estado del borrador (líneas 330-332) se sustituye por el hook:

```tsx
  const {
    draft,
    commitDraft,
    resetDraft,
    undoDraft,
    redoDraft,
    canUndo,
    canRedo,
  } = useDraftHistory<MatrixPlan>(matrixPlan ? clonePlan(matrixPlan) : undefined);
```

Las llamadas se reparten en dos grupos, y no hay una tercera categoría:

**`resetDraft` (tiran la pila, porque el borrador deja de ser el mismo):** línea 355 (`descartarCambios`) y línea 576 (`createDraft`).

```tsx
    resetDraft(matrixPlan ? clonePlan(matrixPlan) : draft);
```

```tsx
    resetDraft(plan);
```

**`commitDraft` (todas las demás):** cada `setDraft(` restante pasa a `commitDraft(`, sin tocar el argumento. Ejemplos literales de los tres estilos que aparecen en el archivo:

```tsx
  // línea 501, forma con función
  const applyToSelection = (patch: Parameters<typeof applyBulkCellEdit>[2]) => {
    commitDraft((current) =>
      current
        ? applyBulkCellEdit(current, effectiveSelection, patch, new Date().toISOString())
        : current,
    );
  };
```

```tsx
  // línea 637, la función que concentra las mutaciones estructurales
  const applyNextDraft = (nextPlan: MatrixPlan) => {
    const next = reconcileMatrixCells(nextPlan);
    commitDraft(next);
    const firstScope = getScopeLeaves(next.scopeTree)[0]?.node;
    const firstArea = getAreaLeaves(next.areas)[0]?.node;
    if (
      selectedCell &&
      next.cells.some(
        (cell) =>
          cell.scopeId === selectedCell.scopeId && cell.areaId === selectedCell.areaId,
      )
    ) {
      return;
    }
    setSelectedCell(
      firstScope && firstArea ? { scopeId: firstScope.id, areaId: firstArea.id } : null,
    );
  };
```

```tsx
  // línea 953, mutación directa sobre el borrador
  const approveFeedback = (cellId: string) => {
    if (!draft) return;
    commitDraft(approveCellFeedback(draft, cellId, new Date().toISOString()));
  };
```

Los atajos de teclado, junto al resto de `useEffect` del componente:

```tsx
  /**
   * `Cmd+Z` dentro del editor deshace el borrador, no el proyecto: el
   * `stopPropagation` impide que el atajo global del Gantt deshaga además el
   * último cambio persistido, que es un estado que el usuario no está mirando.
   */
  useEffect(() => {
    const handleKeyDown = (event: KeyboardEvent) => {
      if (!(event.metaKey || event.ctrlKey)) return;
      if (event.key.toLowerCase() !== "z") return;

      event.preventDefault();
      event.stopPropagation();

      if (event.shiftKey) {
        if (canRedo) redoDraft();
        return;
      }
      if (canUndo) undoDraft();
    };

    window.addEventListener("keydown", handleKeyDown, true);
    return () => window.removeEventListener("keydown", handleKeyDown, true);
  }, [canRedo, canUndo, redoDraft, undoDraft]);
```

Y los dos botones, junto a los controles de la cabecera del editor, para que el atajo no sea la única puerta:

```tsx
        <button
          type="button"
          data-testid="matrix-undo"
          onClick={undoDraft}
          disabled={!canUndo}
          title="Deshacer (⌘Z)"
          className="matrix-toolbar-button"
        >
          Deshacer
        </button>
        <button
          type="button"
          data-testid="matrix-redo"
          onClick={redoDraft}
          disabled={!canRedo}
          title="Rehacer (⌘⇧Z)"
          className="matrix-toolbar-button"
        >
          Rehacer
        </button>
```

> `matrix-toolbar-button` es la clase que usan los botones vecinos de esa cabecera; si el archivo usa otra, se usa la del vecino inmediato, sin inventar estilos nuevos.

Por último, los dos diálogos de confirmación de `deleteScope` (línea 826) y `deleteArea` (línea 852) dejan de mentir: la frase «Esta acción no se puede deshacer» se sustituye por «Se puede deshacer con ⌘Z mientras no guardes.» en los dos `window.confirm`.

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/views/MatrixEditorView.test.tsx --runInBand`
Esperado: PASA — la suite entera, incluidos los tests previos del editor.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/views/MatrixEditorView.tsx v2/src/components/views/MatrixEditorView.test.tsx && git commit -m "feat(remates): borrar en la matriz deja de ser irreversible"
```

---

## Tarea 8: El guardián — ninguna mutación se salta la pila

**Archivos:**
- Crear: `src/components/views/matrixDraftHistory.test.ts`

**Interfaces:**
- Consume: `node:fs` y `node:path`; lee el texto de `src/components/views/MatrixEditorView.tsx`.
- Produce: nada; es un test guardián.

- [ ] **Paso 1: Escribir el test que falla**

```ts
import { readFileSync } from "node:fs";
import path from "node:path";

/**
 * Si el historial cubriera solo algunas operaciones, el usuario no podría
 * predecir qué se deshace — que es peor que no tener deshacer. La única
 * forma de garantizarlo es que `setDraft` no exista.
 */
describe("Detector de mutaciones fuera de la pila (R1)", () => {
  const source = readFileSync(
    path.resolve(__dirname, "MatrixEditorView.tsx"),
    "utf8",
  );

  test("el editor no muta el borrador con setDraft", () => {
    const lineasCulpables = source
      .split("\n")
      .map((line, index) => ({ line, number: index + 1 }))
      .filter((entry) => entry.line.includes("setDraft("))
      .map((entry) => `${entry.number}: ${entry.line.trim()}`);

    expect(lineasCulpables).toEqual([]);
  });

  test("el borrador nace del hook con historial, no de un useState suelto", () => {
    expect(source).toContain("useDraftHistory<MatrixPlan>");
  });

  test("los diálogos de borrado ya no prometen que es irreversible", () => {
    expect(source).not.toContain("no se puede deshacer");
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/views/matrixDraftHistory.test.ts`
Esperado: si la tarea 7 quedó completa, **PASA**. Para verlo fallar por el motivo esperado, revertir temporalmente una sola llamada —por ejemplo la de `approveFeedback` (línea 953)— a `setDraft(` y correr de nuevo: FALLA con `lineasCulpables: ["953: setDraft(approveCellFeedback(draft, cellId, new Date().toISOString()));"]`. Restaurar `commitDraft` acto seguido.

- [ ] **Paso 3: Implementación mínima**

La implementación es la restauración de `commitDraft` en la llamada que se revirtió en el paso 2:

```tsx
  const approveFeedback = (cellId: string) => {
    if (!draft) return;
    commitDraft(approveCellFeedback(draft, cellId, new Date().toISOString()));
  };
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/views src/lib/matrix --runInBand`
Esperado: PASA — todas las suites de vistas y de `lib/matrix`.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/views/matrixDraftHistory.test.ts && git commit -m "test(remates): ninguna operacion del borrador podra saltarse la pila"
```

---

# BLOQUE R2 — Encabezados con abreviatura real

**Corrección al spec, comprobada en el código:** el spec dice que «no existe tal sistema» de abreviaturas. Existe uno **parcial**: `GanttTable.tsx:163-172` define `COMPACT_COLUMN_LABELS` con abreviaturas para **8 de las 14 columnas**, y `GanttTable.tsx:415` las activa **todas a la vez** cuando el panel entero baja de 800 px (`READABLE_TABLE_WIDTH`). Es decir: no depende del ancho de cada columna, y las seis columnas sin entrada se cortan igual. Lo que R2 hace es mover la forma corta a la definición de columna, cubrirlas todas y decidir **columna por columna**. La redacción de R3 se ajusta a este hallazgo en la tarea 13.

## Tarea 9: La función que elige entre título y abreviatura

**Archivos:**
- Crear: `src/lib/gantt/columnLabel.ts`
- Crear: `src/lib/gantt/columnLabel.test.ts`

**Interfaces:**
- Consume: nada.
- Produce:
  - `export const COLUMN_LABEL_CHAR_WIDTH = 7.2`
  - `export const COLUMN_LABEL_PADDING = 16`
  - `export function pickColumnLabel(input: { label: string; shortLabel?: string; width: number; charWidth?: number }): string`

- [ ] **Paso 1: Escribir el test que falla**

```ts
import { pickColumnLabel } from "./columnLabel";

describe("pickColumnLabel (R2: abreviar en vez de cortar)", () => {
  test("con ancho de sobra usa el título completo", () => {
    expect(
      pickColumnLabel({ label: "Predecesoras", shortLabel: "Pred.", width: 180 }),
    ).toBe("Predecesoras");
  });

  test("cuando el título no cabe usa la abreviatura declarada", () => {
    expect(
      pickColumnLabel({ label: "Predecesoras", shortLabel: "Pred.", width: 60 }),
    ).toBe("Pred.");
  });

  test("sin abreviatura declarada se queda con el título: nunca inventa una", () => {
    expect(pickColumnLabel({ label: "Varianza", width: 40 })).toBe("Varianza");
  });

  test("si la abreviatura tampoco cabe, se usa igual: es lo mejor disponible", () => {
    expect(
      pickColumnLabel({ label: "Costo presupuestado", shortLabel: "Costo pres.", width: 30 }),
    ).toBe("Costo pres.");
  });

  test("el ancho justo de la última letra todavía cuenta como que cabe", () => {
    // 5 caracteres × 7,2 px = 36 px, más 16 px de padding = 52 px exactos.
    expect(
      pickColumnLabel({ label: "Fecha", shortLabel: "Fec.", width: 52 }),
    ).toBe("Fecha");
    expect(
      pickColumnLabel({ label: "Fecha", shortLabel: "Fec.", width: 51 }),
    ).toBe("Fec.");
  });

  test("un ancho desconocido no obliga a abreviar", () => {
    expect(
      pickColumnLabel({ label: "Predecesoras", shortLabel: "Pred.", width: 0 }),
    ).toBe("Predecesoras");
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/gantt/columnLabel.test.ts`
Esperado: FALLA con `Cannot find module './columnLabel' from 'src/lib/gantt/columnLabel.test.ts'`

- [ ] **Paso 3: Implementación mínima**

```ts
/**
 * Título largo o forma corta, según el ancho que hay.
 *
 * Antes se recortaba con `text-overflow: ellipsis`, que parte la palabra a la
 * mitad: «Predeceso…» no es un encabezado, es un accidente. La abreviatura la
 * declara cada columna; aquí solo se decide cuál de las dos cabe.
 *
 * El ancho de carácter es una estimación deliberada, no una medición del DOM:
 * medir obligaría a montar la tabla para probar esta decisión, y la decisión
 * no depende del DOM. 7,2 px es el ancho medio de Montserrat 600 en mayúsculas
 * al tamaño del encabezado (`--gantt-column-header-font-size`).
 */
export const COLUMN_LABEL_CHAR_WIDTH = 7.2;

/** Los dos lados del `padding-inline` del encabezado. */
export const COLUMN_LABEL_PADDING = 16;

export function pickColumnLabel(input: {
  label: string;
  shortLabel?: string;
  width: number;
  charWidth?: number;
}): string {
  const { label, shortLabel, width } = input;
  if (!shortLabel || shortLabel === label) return label;
  // Ancho desconocido: no hay motivo para degradar el título.
  if (!Number.isFinite(width) || width <= 0) return label;

  const charWidth = input.charWidth ?? COLUMN_LABEL_CHAR_WIDTH;
  const available = width - COLUMN_LABEL_PADDING;
  return label.length * charWidth <= available ? label : shortLabel;
}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/gantt/columnLabel.test.ts`
Esperado: PASA — 6 tests en verde.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/lib/gantt/columnLabel.ts v2/src/lib/gantt/columnLabel.test.ts && git commit -m "feat(remates): el encabezado elige entre nombre completo y abreviatura"
```

---

## Tarea 10: Cada columna declara su forma corta

**Archivos:**
- Modificar: `src/components/gantt/table/ColumnSelector.tsx:19-33` (interfaz `ColumnConfig`)
- Modificar: `src/components/gantt/table/GanttTable.tsx:113-131` (`DEFAULT_COLUMNS`)
- Crear: `src/components/gantt/table/columnShortLabels.test.ts`

**Interfaces:**
- Consume: `DEFAULT_COLUMNS: ColumnConfig[]` exportado desde `src/components/gantt/table/GanttTable.tsx:113`.
- Produce: `ColumnConfig` gana `shortLabelEs?: string` y `shortLabelEn?: string`.

- [ ] **Paso 1: Escribir el test que falla**

```ts
import { DEFAULT_COLUMNS } from "./GanttTable";

describe("Abreviaturas de columna (R2)", () => {
  test("ninguna abreviatura es más larga que su título: sería un error de datos", () => {
    const invertidas = DEFAULT_COLUMNS.filter((column) => {
      const es = column.shortLabelEs;
      const en = column.shortLabelEn;
      return (
        (es != null && es.length > (column.labelEs ?? column.label).length) ||
        (en != null && en.length > (column.labelEn ?? column.label).length)
      );
    }).map((column) => column.key);

    expect(invertidas).toEqual([]);
  });

  test("toda columna cuyo título pase de 8 caracteres declara abreviatura", () => {
    const sinAbreviar = DEFAULT_COLUMNS.filter(
      (column) =>
        (column.labelEs ?? column.label).length > 8 && column.shortLabelEs == null,
    ).map((column) => column.key);

    expect(sinAbreviar).toEqual([]);
  });

  test("las columnas cortas no declaran abreviatura: no la necesitan", () => {
    expect(DEFAULT_COLUMNS.find((column) => column.key === "id")?.shortLabelEs).toBeUndefined();
    expect(DEFAULT_COLUMNS.find((column) => column.key === "wbs")?.shortLabelEs).toBeUndefined();
  });

  test("las abreviaturas históricas se conservan tal cual", () => {
    const porClave = new Map(DEFAULT_COLUMNS.map((column) => [column.key, column]));
    expect(porClave.get("duration")?.shortLabelEs).toBe("Dur.");
    expect(porClave.get("predecessors")?.shortLabelEs).toBe("Pred.");
    expect(porClave.get("critical")?.shortLabelEs).toBe("Crít.");
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/gantt/table/columnShortLabels.test.ts -t "pase de 8 caracteres"`
Esperado: FALLA con `sinAbreviar: ["uniqueId", "duration", "predecessors", "progress", "budgetedCost", "actualCost", "variance"]` frente al `[]` esperado.

- [ ] **Paso 3: Implementación mínima**

En `src/components/gantt/table/ColumnSelector.tsx`, dos campos nuevos en `ColumnConfig`, después de `labelEs`:

```ts
export interface ColumnConfig {
  key: string;
  label: string;
  labelEn?: string;
  labelEs?: string;
  /**
   * Forma corta del título. Si no se declara, la columna no se abrevia — pero
   * tampoco se corta a mitad de palabra: se queda con el título completo.
   */
  shortLabelEs?: string;
  shortLabelEn?: string;
  width: number;
  align: "left" | "right" | "center";
  defaultVisible: boolean;
  sourceKey?: string;
  dataType?: string;
  readOnly?: boolean;
  group?: string;
  calculationSpec?: MppCalculationSpec;
}
```

En `src/components/gantt/table/GanttTable.tsx`, `DEFAULT_COLUMNS` completo:

```ts
export const DEFAULT_COLUMNS: ColumnConfig[] = [
  { key: "id", label: "ID", labelEn: "ID", labelEs: "ID", width: 50, align: "right", defaultVisible: true },
  { key: "uniqueId", label: "Id. único", labelEn: "Unique ID", labelEs: "Id. único", shortLabelEs: "UID", shortLabelEn: "UID", width: 90, align: "right", defaultVisible: true },
  { key: "wbs", label: "EDT", labelEn: "WBS", labelEs: "EDT", width: 80, align: "left", defaultVisible: true },
  { key: "name", label: "Actividad", labelEn: "Activity", labelEs: "Actividad", shortLabelEs: "Activid.", shortLabelEn: "Activity", width: 220, align: "left", defaultVisible: true },
  { key: "summary", label: "Resumen", labelEn: "Summary", labelEs: "Resumen", width: 80, align: "center", defaultVisible: true },
  { key: "duration", label: "Duración", labelEn: "Duration", labelEs: "Duración", shortLabelEs: "Dur.", shortLabelEn: "Dur.", width: 90, align: "right", defaultVisible: true },
  { key: "start", label: "Comienzo", labelEn: "Start", labelEs: "Comienzo", width: 110, align: "left", defaultVisible: true },
  { key: "finish", label: "Fin", labelEn: "Finish", labelEs: "Fin", width: 110, align: "left", defaultVisible: true },
  { key: "predecessors", label: "Predecesoras", labelEn: "Predecessors", labelEs: "Predecesoras", shortLabelEs: "Pred.", shortLabelEn: "Pred.", width: 180, align: "left", defaultVisible: true },
  { key: "progress", label: "% completado", labelEn: "% Complete", labelEs: "% completado", shortLabelEs: "%", shortLabelEn: "%", width: 100, align: "right", defaultVisible: true },
  { key: "critical", label: "Crítica", labelEn: "Critical", labelEs: "Crítica", shortLabelEs: "Crít.", shortLabelEn: "Crit.", width: 80, align: "center", defaultVisible: true },
  { key: "budgetedCost", label: "Costo presupuestado", labelEn: "Budgeted Cost", labelEs: "Costo presupuestado", shortLabelEs: "Costo pres.", shortLabelEn: "Budg. cost", width: 140, align: "right", defaultVisible: false },
  { key: "actualCost", label: "Costo real", labelEn: "Actual Cost", labelEs: "Costo real", shortLabelEs: "C. real", shortLabelEn: "Act. cost", width: 110, align: "right", defaultVisible: false },
  { key: "variance", label: "Varianza", labelEn: "Variance", labelEs: "Varianza", width: 110, align: "right", defaultVisible: false },
];
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/gantt/table/columnShortLabels.test.ts`
Esperado: PASA — 4 tests en verde.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/gantt/table/ColumnSelector.tsx v2/src/components/gantt/table/GanttTable.tsx v2/src/components/gantt/table/columnShortLabels.test.ts && git commit -m "feat(remates): la forma corta vive en la definicion de la columna"
```

---

## Tarea 11: El encabezado abrevia por ancho de columna, no por ancho de panel

**Archivos:**
- Modificar: `src/components/gantt/table/GanttTable.tsx:163-172` (borrar `COMPACT_COLUMN_LABELS`), `:415` (borrar `useCompactColumnLabels`), `:1253-1269` (el `map` del `<thead>`)
- Modificar: `src/components/gantt/table/ColumnHeader.tsx:5-15, 24-33, 66-113` (props, título y render)
- Test: `src/components/gantt/table/GanttTable.test.tsx`

**Interfaces:**
- Consume: `pickColumnLabel({ label, shortLabel, width })` de `src/lib/gantt/columnLabel.ts`; `ColumnConfig.shortLabelEs` / `shortLabelEn` de la tarea 10.
- Produce: `ColumnHeaderProps` gana `shortLabel?: string`; `ColumnHeader` sigue exponiendo `data-testid="column-header"` y añade `data-full-label` con el título completo.

- [ ] **Paso 1: Escribir el test que falla**

Se añade al final de `src/components/gantt/table/GanttTable.test.tsx`:

```tsx
describe("GanttTable · encabezados que abrevian en vez de cortarse (R2)", () => {
  test("una columna estrecha muestra la abreviatura, no el título partido", () => {
    renderGanttTable({ columnWidths: { predecessors: 60 } });

    const encabezado = screen
      .getAllByTestId("column-header")
      .find((node) => node.getAttribute("data-full-label") === "Predecesoras");

    expect(encabezado).toBeDefined();
    expect(encabezado).toHaveTextContent("Pred.");
  });

  test("el nombre completo sigue accesible en el title del encabezado", () => {
    renderGanttTable({ columnWidths: { predecessors: 60 } });

    const encabezado = screen
      .getAllByTestId("column-header")
      .find((node) => node.getAttribute("data-full-label") === "Predecesoras");

    expect(encabezado).toHaveAttribute("title", expect.stringContaining("Predecesoras"));
  });

  test("con ancho de sobra se muestra el título completo", () => {
    renderGanttTable({ columnWidths: { predecessors: 200 } });

    const encabezado = screen
      .getAllByTestId("column-header")
      .find((node) => node.getAttribute("data-full-label") === "Predecesoras");

    expect(encabezado).toHaveTextContent("Predecesoras");
  });
});
```

> Nota para quien ejecute: `renderGanttTable` es el helper de esta suite. Si no acepta `columnWidths`, pásale los anchos por la vía que la suite ya use para fijar anchos de columna (busca `taskColumnSettings` o `widths` dentro del archivo) y ajusta la llamada; el aserto no cambia.

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/gantt/table/GanttTable.test.tsx -t "muestra la abreviatura" --runInBand`
Esperado: FALLA con `expect(received).toBeDefined()` — ningún encabezado tiene `data-full-label`, porque el atributo todavía no existe.

- [ ] **Paso 3: Implementación mínima**

En `src/components/gantt/table/ColumnHeader.tsx`, las props y el render:

```tsx
interface ColumnHeaderProps {
  label: string;
  /** Forma corta declarada por la columna. Sin ella, no se abrevia. */
  shortLabel?: string;
  locale?: UILocale;
  width?: number;
  align?: "left" | "right" | "center";
  onResize?: (newWidth: number) => void;
  isResizable?: boolean;
  calculationSpec?: MppCalculationSpec;
}
```

```tsx
export default function ColumnHeader({
  label,
  shortLabel,
  locale = "es",
  width,
  align = "left",
  onResize,
  isResizable = true,
  calculationSpec,
}: ColumnHeaderProps) {
```

El import nuevo:

```tsx
import { pickColumnLabel } from "@/lib/gantt/columnLabel";
```

El título mostrado y el `title` del `<th>` (se sustituye el bloque que hoy calcula `title` a partir de `calculationSpec`, conservando lo que ya hacía):

```tsx
  const displayLabel = pickColumnLabel({ label, shortLabel, width: width ?? 0 });
  const calculationTitle = calculationSpec
    ? [
        calculationSpec.isCalculated
          ? locale === "en"
            ? "Calculated"
            : "Calculada"
          : locale === "en"
            ? "Input"
            : "Entrada",
        locale === "en"
          ? `Origin: ${calculationSpec.sourceOfTruth ?? "import"}`
          : `Origen: ${calculationSpec.sourceOfTruth ?? "importación"}`,
        calculationSpec.formula
          ? `${locale === "en" ? "Formula" : "Formula"}: ${calculationSpec.formula}`
          : undefined,
        calculationSpec.lastCalculatedAt
          ? `${locale === "en" ? "Last calculation" : "Último cálculo"}: ${calculationSpec.lastCalculatedAt}`
          : undefined,
        calculationSpec.unsupportedReason,
      ]
        .filter(Boolean)
        .join(" | ")
    : undefined;
  // El nombre completo nunca se pierde: abreviar la cabecera no puede costar
  // saber de qué columna se trata.
  const title = calculationTitle ? `${label} | ${calculationTitle}` : label;
```

Y el `<th>`:

```tsx
    <th
      ref={thRef}
      data-testid="column-header"
      data-full-label={label}
      className="gantt-column-header"
      data-align={align}
      title={title}
    >
      {displayLabel}
```

En `src/components/gantt/table/GanttTable.tsx` se borran `COMPACT_COLUMN_LABELS` (líneas 163-172) y la constante `useCompactColumnLabels` (línea 415), y el `map` del `<thead>` queda:

```tsx
            {displayColumns.map((col) => {
              const label =
                effectiveLocale === "en"
                  ? col.labelEn ?? col.label
                  : col.labelEs ?? col.label;
              const shortLabel =
                effectiveLocale === "en" ? col.shortLabelEn : col.shortLabelEs;
              const width = displayColumnWidths[col.key] ?? col.width;
              return (
                <ColumnHeader
                  key={col.key}
                  label={label}
                  shortLabel={shortLabel}
                  locale={effectiveLocale}
                  width={width}
                  align={col.align}
                  calculationSpec={col.calculationSpec}
                  onResize={(newWidth) => handleResize(col.key, newWidth)}
                />
              );
            })}
```

> Si al borrar `useCompactColumnLabels` queda `tablePanelWidth` sin ningún otro consumidor, se deja tal cual: lo usan otras decisiones de layout en el mismo archivo. Comprobarlo con `grep -n "tablePanelWidth" src/components/gantt/table/GanttTable.tsx` antes de borrar nada más.

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/gantt/table --runInBand`
Esperado: PASA — todas las suites de `table/`. Si algún test previo esperaba las etiquetas compactas por ancho de panel, se actualiza en esta misma tarea al comportamiento nuevo.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/gantt/table/ColumnHeader.tsx v2/src/components/gantt/table/GanttTable.tsx v2/src/components/gantt/table/GanttTable.test.tsx && git commit -m "feat(remates): los encabezados abrevian por columna y no se parten a la mitad"
```

---

# BLOQUE R3 — La documentación deja de mentir

Sin código de producción. La disciplina se mantiene con un test que lee los documentos: la sincronía entre `EXPERIMENTS.md` y `DESIGN.md` es verificable de forma mecánica porque la tabla de experimentos declara, en su columna «Origen», el hallazgo de `DESIGN.md` que cierra.

## Tarea 12: El test que cruza experimentos con hallazgos

**Archivos:**
- Crear: `src/lib/docs/docsConsistency.ts`
- Crear: `src/lib/docs/docsConsistency.test.ts`
- Modificar: `docs/DESIGN.md` (las filas que el test señale)

**Interfaces:**
- Consume: `node:fs`, `node:path`.
- Produce:
  - `export const REPO_DOCS_DIR: string`
  - `export function readDoc(name: "EXPERIMENTS.md" | "DESIGN.md" | "PRODUCT.md"): string`
  - `export interface ShippedExperiment { id: string; findings: number[]; status: string }`
  - `export function shippedExperiments(experimentsDoc: string): ShippedExperiment[]`
  - `export function designFindingStatus(designDoc: string, finding: number): string | null`

- [ ] **Paso 1: Escribir el test que falla**

```ts
import {
  designFindingStatus,
  readDoc,
  shippedExperiments,
} from "./docsConsistency";

describe("Sincronía entre EXPERIMENTS.md y DESIGN.md (R3)", () => {
  const experiments = readDoc("EXPERIMENTS.md");
  const design = readDoc("DESIGN.md");

  test("se leen experimentos ya enviados con su hallazgo de origen", () => {
    const enviados = shippedExperiments(experiments);

    expect(enviados.length).toBeGreaterThan(20);
    expect(enviados.find((item) => item.id === "E1")?.findings).toEqual([2]);
    expect(enviados.find((item) => item.id === "E45")?.findings).toEqual([7, 22, 40]);
  });

  test("ningún hallazgo cerrado por un experimento enviado sigue marcado open", () => {
    const mentiras = shippedExperiments(experiments).flatMap((experiment) =>
      experiment.findings
        .filter((finding) => designFindingStatus(design, finding) === "open")
        .map((finding) => `#${finding} sigue open pero ${experiment.id} está shipped`),
    );

    expect(mentiras).toEqual([]);
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/docs/docsConsistency.test.ts`
Esperado: FALLA con `Cannot find module './docsConsistency' from 'src/lib/docs/docsConsistency.test.ts'`

- [ ] **Paso 3: Implementación mínima**

`src/lib/docs/docsConsistency.ts`:

```ts
import { readFileSync } from "node:fs";
import path from "node:path";

/**
 * Los documentos leídos como datos, no como prosa.
 *
 * La auditoría del 2026-08-08 encontró filas marcadas `open` en `DESIGN.md`
 * cuyo experimento llevaba días `shipped`. Quien lea el documento suelto
 * concluye que hay deuda que no existe, y la siguiente auditoría arranca con
 * datos equivocados. La única forma de que no vuelva a pasar es que la
 * contradicción rompa la suite.
 */
export const REPO_DOCS_DIR = path.resolve(__dirname, "../../../../docs");

export function readDoc(
  name: "EXPERIMENTS.md" | "DESIGN.md" | "PRODUCT.md",
): string {
  return readFileSync(path.join(REPO_DOCS_DIR, name), "utf8");
}

export interface ShippedExperiment {
  id: string;
  /** Hallazgos de DESIGN.md que el experimento cierra. */
  findings: number[];
  status: string;
}

export function shippedExperiments(experimentsDoc: string): ShippedExperiment[] {
  const result: ShippedExperiment[] = [];

  for (const line of experimentsDoc.split("\n")) {
    if (!line.startsWith("|")) continue;
    const cells = line.split("|").map((cell) => cell.trim());
    // | (vacío) | ID | Cambio | Origen | ... | Estado | (vacío)
    const id = cells[1];
    if (!/^E\d+$/.test(id ?? "")) continue;

    const status = cells[cells.length - 2] ?? "";
    if (!status.includes("shipped")) continue;

    const findings = [...(cells[3] ?? "").matchAll(/#(\d+)/g)].map((match) =>
      Number(match[1]),
    );
    result.push({ id, findings, status });
  }

  return result;
}

export function designFindingStatus(
  designDoc: string,
  finding: number,
): string | null {
  for (const line of designDoc.split("\n")) {
    if (!line.startsWith("|")) continue;
    const cells = line.split("|").map((cell) => cell.trim());
    if (cells[1] !== String(finding)) continue;

    const status = cells[cells.length - 2] ?? "";
    // «open (E4)» y «open» son lo mismo para esto; «**done**» y «parcial» no.
    return status.startsWith("open") ? "open" : status;
  }
  return null;
}
```

Después, se corre el test para obtener la lista exacta de filas mentirosas y se edita `docs/DESIGN.md` fila por fila: en cada fila señalada, la última celda `| open |` (o `| open (EN) |`) pasa a `| **done** — cerrado por EN el <fecha del experimento> |`, tomando `EN` y la fecha del `shipped YYYY-MM-DD` de la fila correspondiente de `EXPERIMENTS.md`. No se toca ninguna otra columna, ni las filas marcadas `parcial`, que siguen siendo verdad.

Ejemplo del cambio, para la fila 4 (hallazgo #4, cerrado por E4 `shipped 2026-08-06`):

```markdown
| 4 | A | Importación **sin progreso, sin timeout y sin cancelar**: … | 1 Visibilidad | **3** | Progreso real por fases (subiendo → analizando → guardando), timeout con mensaje y botón cancelar | **done** — cerrado por E4 el 2026-08-06 |
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/docs/docsConsistency.test.ts`
Esperado: PASA — `mentiras` vacío.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/lib/docs/docsConsistency.ts v2/src/lib/docs/docsConsistency.test.ts docs/DESIGN.md && git commit -m "docs(remates): design deja de mostrar como abierto lo que ya se cerro"
```

---

## Tarea 13: Las dos afirmaciones falsas de EXPERIMENTS.md

**Archivos:**
- Modificar: `docs/EXPERIMENTS.md:94` (la excusa de E38) y `docs/EXPERIMENTS.md:245-248` (la cobertura del deshacer)
- Test: `src/lib/docs/docsConsistency.test.ts` (se le añade un `describe`)

**Interfaces:**
- Consume: `readDoc("EXPERIMENTS.md")` de `src/lib/docs/docsConsistency.ts`.
- Produce: nada nuevo.

- [ ] **Paso 1: Escribir el test que falla**

Se añade a `src/lib/docs/docsConsistency.test.ts`:

```ts
describe("EXPERIMENTS.md dice la verdad sobre sí mismo (R3)", () => {
  const experiments = readDoc("EXPERIMENTS.md");

  test("E38 ya no atribuye a los encabezados un sistema responsivo que no tenían", () => {
    expect(experiments).not.toContain("tienen sistema responsivo propio con abreviaturas");
  });

  test("la cobertura del deshacer solo declara abiertos los casos que siguen abiertos", () => {
    expect(experiments).not.toContain("`handleSyncMatrixFromGantt`, el reset de columnas");
    expect(experiments).toContain("el borrado de proyecto (permanente en servidor)");
  });

  test("el borrado de proyecto se declara irreversible por diseño, no como deuda", () => {
    expect(experiments).toContain("irreversible por diseño");
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/docs/docsConsistency.test.ts -t "sistema responsivo"`
Esperado: FALLA con `expect(received).not.toContain(expected)` — la cadena está en la línea 94.

- [ ] **Paso 3: Implementación mínima**

**Error 2 — la excusa de E38.** En `docs/EXPERIMENTS.md:94`, la última celda de la fila E38 pasa de:

```markdown
| **parcial** — barra hecha (E38a); encabezados comprimidos de la tabla siguen open (tienen sistema responsivo propio con abreviaturas) |
```

a:

```markdown
| **shipped 2026-08-08** — barra hecha (E38a); los encabezados eran **truncamiento CSS** (`ellipsis`) más un mapa parcial de 8 abreviaturas que se activaba por ancho de panel, no por columna. R2 lo cierra: cada columna declara su forma corta y se elige por su propio ancho |
```

**Error 1 — la cobertura subestimada.** En `docs/EXPERIMENTS.md:245-248`, el párrafo:

```markdown
**Cobertura declarada — lo que NO cubre (queda abierto):** editar un recurso o una partida (sobrescriben sin
copia de seguridad), `handleSyncMatrixFromGantt`, el reset de columnas en las tres tablas, los borrados de
`MatrixEditorView` (piden confirmación pero no son deshacibles) y el borrado de proyecto (permanente en
servidor). Por eso el hallazgo #27 queda como **parcial**, no resuelto.
```

pasa a:

```markdown
**Cobertura declarada — corregida el 2026-08-08.** El párrafo original quedó congelado el 2026-08-05 y
declaraba cinco casos sin cubrir; **cuatro ya estaban resueltos** vía `runUndoable` cuando se escribió:
editar recurso (`GanttView.tsx:736`), editar partida (`:773`), `handleSyncMatrixFromGantt` (`:1033`) y el
reset de columnas en las tres tablas (`:1046`, `:1060`, `:1115`).

Queda **un** caso realmente abierto: los borrados de `MatrixEditorView`, que piden confirmación y no eran
deshacibles — **cerrado por R1**, que le da al borrador su propia pila. Y el borrado de proyecto (permanente
en servidor, `project.ts:597`), que es **irreversible por diseño** y no cuenta como deuda.
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/docs/docsConsistency.test.ts`
Esperado: PASA — las dos suites del archivo.

- [ ] **Paso 5: Commit**

```bash
git add docs/EXPERIMENTS.md v2/src/lib/docs/docsConsistency.test.ts && git commit -m "docs(remates): experiments deja de subestimarse y de inventar un sistema que no existia"
```

---

## Tarea 14: PRODUCT.md — C3 revertido con motivo

**Archivos:**
- Modificar: `docs/PRODUCT.md:52` (el corte C3), `:85-91` (Outcome Roadmap), `:198` (el registro de cortes)
- Test: `src/lib/docs/docsConsistency.test.ts` (se le añade un `describe`)

**Interfaces:**
- Consume: `readDoc("PRODUCT.md")` de `src/lib/docs/docsConsistency.ts`.
- Produce: nada nuevo.

- [ ] **Paso 1: Escribir el test que falla**

```ts
describe("PRODUCT.md registra C3 revertido (R3)", () => {
  const product = readDoc("PRODUCT.md");

  test("la reversión de C3 está escrita con su motivo", () => {
    expect(product).toContain("Revertido 2026-08-08");
    expect(product).toContain("editor de dependencias");
  });

  test("el Outcome Roadmap ya no anuncia el recorte a 9 vistas como propuesto", () => {
    expect(product).not.toContain("| De 14 vistas a 9 (C1, C2, C3) | Emocional — sobrecarga | **3** | propuesto |");
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/docs/docsConsistency.test.ts -t "reversión de C3"`
Esperado: FALLA con `expect(received).toContain("Revertido 2026-08-08")`.

- [ ] **Paso 3: Implementación mínima**

En `docs/PRODUCT.md:52`, la fila del corte C3:

```markdown
| C3 | **Sacar «Diagrama Red» de la barra principal** | 207 caracteres de contenido y ningún job de CUSTOMER.md lo pide; es paridad con MS Project, no valor de obra | **Revertido 2026-08-08** — P5 le da un editor de dependencias: deja de ser paridad vacía y pasa a ser el sitio donde se dibujan las relaciones entre tareas. El motivo original —«ningún contenido»— dejó de ser cierto |
```

En `docs/PRODUCT.md:91`, la fila del Outcome Roadmap:

```markdown
| De 14 vistas a 11 (C1, C2; C3 revertido) | Emocional — sobrecarga | **3** | **hecho 2026-08-08** — el menú quedó en 11 entradas; el Diagrama de Red vuelve a la barra con el editor de dependencias de P5 |
```

En `docs/PRODUCT.md:198`, la fila del registro de cortes:

```markdown
| C3 · Sacar Diagrama de Red de la barra | ↩️ revertido — P5 le da contenido real (editor de dependencias) |
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/docs --runInBand`
Esperado: PASA — la suite de `docs/` completa.

- [ ] **Paso 5: Commit**

```bash
git add docs/PRODUCT.md v2/src/lib/docs/docsConsistency.test.ts && git commit -m "docs(remates): c3 se revierte con motivo escrito, no en silencio"
```

---

# BLOQUE R4 — Corregir a mano la ubicación detectada

P3 dejó el diccionario (`dictionary.ts`) y la cascada que lo consulta (`taskLocation.ts:37`), pero **ninguna pantalla lo alimenta**. Faltan tres piezas: que el diccionario se guarde con el proyecto, que la Línea de Balance lo consulte, y una pantalla para escribirlo.

## Tarea 15: El diccionario se guarda con el proyecto

**Archivos:**
- Modificar: `src/app/actions/project.ts:40-59` (`ProjectData`), `:185-204` (`SerializedProjectData`), `:206-232` (`serializeProjectData`), `:234-260` (`deserializeProjectData`)
- Test: `src/app/actions/project.test.ts`

**Interfaces:**
- Consume: `DetectionDictionary` de `src/lib/scheduling/detection/dictionary.ts`.
- Produce: `ProjectData.detectionDictionary?: DetectionDictionary` y su equivalente en `SerializedProjectData`.

- [ ] **Paso 1: Escribir el test que falla**

Se añade al final de `src/app/actions/project.test.ts`:

```ts
describe("ProjectData · el diccionario de correcciones viaja con el proyecto (R4)", () => {
  test("una corrección guardada sobrevive al viaje de ida y vuelta", () => {
    const dictionary = rememberCorrection(EMPTY_DETECTION_DICTIONARY, {
      kind: "ubicacion",
      name: "Instalación de redes secas",
      value: "4",
      note: "Va en el piso 4, no en obra general",
      recordedAt: "2026-08-08T10:00:00.000Z",
    });

    const serialized = serializeProjectDataForTest({
      ...baseProjectData(),
      detectionDictionary: dictionary,
    });

    expect(serialized.detectionDictionary?.corrections).toHaveLength(1);
    expect(serialized.detectionDictionary?.corrections[0].value).toBe("4");

    const restored = deserializeProjectDataForTest("proj-1", {
      name: "Estación 16",
      project_data: serialized,
    });

    expect(restored.detectionDictionary).toEqual(dictionary);
  });

  test("un proyecto viejo sin diccionario se lee como diccionario vacío", () => {
    const serialized = serializeProjectDataForTest(baseProjectData());
    delete (serialized as { detectionDictionary?: unknown }).detectionDictionary;

    const restored = deserializeProjectDataForTest("proj-2", {
      name: "Antiguo",
      project_data: serialized,
    });

    expect(restored.detectionDictionary).toEqual(EMPTY_DETECTION_DICTIONARY);
  });
});
```

> Nota para quien ejecute: `serializeProjectData` y `deserializeProjectData` son privadas del módulo. Si la suite ya las prueba por alguna vía (busca `serializeProjectData` en el archivo de test), úsala. Si no, se exportan con el prefijo de prueba explícito —`export { serializeProjectData as serializeProjectDataForTest, deserializeProjectData as deserializeProjectDataForTest }` al final de `project.ts`— y se importan con ese nombre. `baseProjectData()` es el fixture de proyecto que la suite ya construye; reutilízalo.

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/app/actions/project.test.ts -t "sobrevive al viaje de ida y vuelta" --runInBand`
Esperado: FALLA con `Object literal may only specify known properties, and 'detectionDictionary' does not exist in type 'ProjectData'` en tiempo de compilación, o `expect(received).toHaveLength(1)` con `received: undefined` si el proyecto compila con `ts-jest` permisivo.

- [ ] **Paso 3: Implementación mínima**

En `src/app/actions/project.ts`, el import nuevo junto a los demás tipos:

```ts
import {
  EMPTY_DETECTION_DICTIONARY,
  type DetectionDictionary,
} from "@/lib/scheduling/detection/dictionary";
```

En `ProjectData`, después de `matrixPlan?: MatrixPlan;`:

```ts
  /**
   * Lo que el usuario corrigió a mano sobre la detección automática. Viaja
   * dentro de `project_data`: no necesita columna ni migración, y el motor
   * lo recibe como argumento porque no sabe dónde se guarda.
   */
  detectionDictionary?: DetectionDictionary;
```

La misma línea, en `SerializedProjectData`, después de su `matrixPlan?: MatrixPlan;`:

```ts
  detectionDictionary?: DetectionDictionary;
```

En `serializeProjectData`, después de `matrixPlan: data.matrixPlan,`:

```ts
    detectionDictionary: data.detectionDictionary ?? EMPTY_DETECTION_DICTIONARY,
```

En `deserializeProjectData`, después de `matrixPlan: pd.matrixPlan,`:

```ts
    detectionDictionary: pd.detectionDictionary ?? EMPTY_DETECTION_DICTIONARY,
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/app/actions/project.test.ts --runInBand`
Esperado: PASA — la suite completa de acciones de proyecto.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/app/actions/project.ts v2/src/app/actions/project.test.ts && git commit -m "feat(remates): las correcciones del usuario se guardan con el proyecto"
```

---

## Tarea 16: La Línea de Balance respeta las correcciones

**Archivos:**
- Modificar: `src/lib/scheduling/lob.ts:475-482` (`detectTaskUnit`), `:570-575` (`generateTextLOBFromTasks`), `:762-769` (`generateAutomaticLOBFromTasks`)
- Test: `src/lib/scheduling/lob.test.ts`

**Interfaces:**
- Consume: `resolveTaskLocation(task, tasks, dictionary?, nameByWbs?)` de `src/lib/scheduling/detection/taskLocation.ts`; `DetectionDictionary` de `src/lib/scheduling/detection/dictionary.ts`.
- Produce: `generateAutomaticLOBFromTasks(tasks: GanttTask[], matrixPlan?: MatrixPlan, dictionary?: DetectionDictionary): AutomaticLOBResult` — el tercer parámetro es opcional, así que ninguna llamada existente se rompe.

- [ ] **Paso 1: Escribir el test que falla**

Se añade al `describe("generateAutomaticLOBFromTasks", ...)` que ya existe en `src/lib/scheduling/lob.test.ts`:

```ts
  test("una corrección guardada gana a la detección automática (R4)", () => {
    const tasks = [
      lobTask({ id: 1, name: "Mampostería Piso 1" }),
      lobTask({ id: 2, name: "Mampostería Piso 2" }),
      lobTask({ id: 3, name: "Instalación de redes secas" }),
    ];

    const sinCorregir = generateAutomaticLOBFromTasks(tasks);
    const unidadesSinCorregir = sinCorregir.units.filter(
      (unit) => unit.unitName === "Piso 3",
    );
    expect(unidadesSinCorregir).toHaveLength(0);

    const dictionary = rememberCorrection(EMPTY_DETECTION_DICTIONARY, {
      kind: "ubicacion",
      name: "Instalación de redes secas",
      value: "3",
      note: "Va en el piso 3",
      recordedAt: "2026-08-08T10:00:00.000Z",
    });

    const corregido = generateAutomaticLOBFromTasks(tasks, undefined, dictionary);

    expect(
      corregido.units.some((unit) => unit.unitName === "Piso 3"),
    ).toBe(true);
  });

  test("corregir una tarea no toca a las demás con nombre parecido (R4)", () => {
    const tasks = [
      lobTask({ id: 1, name: "Mampostería Piso 1" }),
      lobTask({ id: 2, name: "Mampostería Piso 2" }),
    ];

    const dictionary = rememberCorrection(EMPTY_DETECTION_DICTIONARY, {
      kind: "ubicacion",
      name: "Mampostería Piso 1",
      value: "7",
      note: "Estaba mal numerada en el .mpp",
      recordedAt: "2026-08-08T10:00:00.000Z",
    });

    const corregido = generateAutomaticLOBFromTasks(tasks, undefined, dictionary);
    const nombres = corregido.units.map((unit) => unit.unitName);

    expect(nombres).toContain("Piso 7");
    expect(nombres).toContain("Piso 2");
    expect(nombres).not.toContain("Piso 1");
  });
```

> Nota para quien ejecute: `lobTask` es el constructor de tareas que la suite ya usa (búscalo al principio del archivo; puede llamarse distinto). Los imports de `rememberCorrection` y `EMPTY_DETECTION_DICTIONARY` se añaden a la cabecera del archivo de test.

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/scheduling/lob.test.ts -t "gana a la detección automática" --runInBand`
Esperado: FALLA con `Expected 2 arguments, but got 3` en tiempo de compilación, o `expect(received).toBe(true) // Received: false` — la corrección se pasa pero nadie la mira.

- [ ] **Paso 3: Implementación mínima**

En `src/lib/scheduling/lob.ts`, el import nuevo:

```ts
import type { DetectionDictionary } from "./detection/dictionary";
```

`detectTaskUnit` deja de pasar `undefined` a ciegas:

```ts
function detectTaskUnit(
  task: GanttTask,
  tasks: GanttTask[],
  nameByWbs: Map<string, string>,
  dictionary?: DetectionDictionary,
): { label: string; key: string; index: number } | null {
  const { location } = resolveTaskLocation(task, tasks, dictionary, nameByWbs);
  return location ? describeUnit(location) : null;
}
```

`generateTextLOBFromTasks` recibe el diccionario y lo pasa:

```ts
function generateTextLOBFromTasks(
  tasks: GanttTask[],
  matrixPlan?: MatrixPlan,
  dictionary?: DetectionDictionary,
): AutomaticLOBResult {
  // Una sola vez para todo el cronograma: la herencia lo consulta por tarea.
  const nameByWbs = buildWbsNameMap(tasks);
  const candidates = tasks
    .filter((task) => !task.isSummary && !task.isMilestone)
    .map((task) => ({ task, unit: detectTaskUnit(task, tasks, nameByWbs, dictionary) }))
    .filter((entry): entry is { task: GanttTask; unit: { label: string; key: string; index: number } } => entry.unit !== null);
```

Y la puerta de entrada:

```ts
export function generateAutomaticLOBFromTasks(
  tasks: GanttTask[],
  matrixPlan?: MatrixPlan,
  /**
   * Lo que el usuario corrigió a mano. Va antes que lo automático por la
   * cascada de P3: una corrección humana no se equivoca por parecido.
   */
  dictionary?: DetectionDictionary,
): AutomaticLOBResult {
  const textResult = generateTextLOBFromTasks(tasks, matrixPlan, dictionary);
  if (textResult.activities.length > 0) return textResult;
  return generateWBSLOBFromTasks(tasks);
}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/scheduling/lob.test.ts --runInBand`
Esperado: PASA — la suite completa de la Línea de Balance.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/lib/scheduling/lob.ts v2/src/lib/scheduling/lob.test.ts && git commit -m "feat(remates): la linea de balance respeta lo que el usuario corrigio"
```

---

## Tarea 17: La pantalla donde se corrige

**Archivos:**
- Crear: `src/components/lob/LocationCorrectionPanel.tsx`
- Crear: `src/components/lob/LocationCorrectionPanel.test.tsx`

**Interfaces:**
- Consume: `GanttTask` de `src/components/gantt/types`; `resolveTaskLocation` de `src/lib/scheduling/detection/taskLocation.ts`; `formatLocationLabel` de `src/lib/scheduling/detection/location.ts`; `DetectionDictionary` de `src/lib/scheduling/detection/dictionary.ts`.
- Produce:
  - `export interface LocationCorrectionPanelProps { tasks: GanttTask[]; dictionary: DetectionDictionary; onCorrect: (input: { taskName: string; value: string; note: string }) => void }`
  - `export default function LocationCorrectionPanel(props: LocationCorrectionPanelProps): JSX.Element`

- [ ] **Paso 1: Escribir el test que falla**

```tsx
/**
 * @jest-environment jsdom
 */

import "@testing-library/jest-dom";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import LocationCorrectionPanel from "./LocationCorrectionPanel";
import { EMPTY_DETECTION_DICTIONARY } from "@/lib/scheduling/detection/dictionary";
import type { GanttTask } from "@/components/gantt/types";

function task(overrides: Partial<GanttTask> & { id: string | number }): GanttTask {
  return {
    name: `Tarea ${overrides.id}`,
    start: new Date("2026-01-05T08:00:00"),
    finish: new Date("2026-01-09T17:00:00"),
    duration: 5,
    progress: 0,
    isCritical: false,
    isMilestone: false,
    isSummary: false,
    outlineLevel: 1,
    dependencies: [],
    ...overrides,
  };
}

describe("LocationCorrectionPanel (R4)", () => {
  test("muestra la ubicación que el motor asignó a cada tarea", () => {
    render(
      <LocationCorrectionPanel
        tasks={[task({ id: 1, name: "Mampostería Piso 4" })]}
        dictionary={EMPTY_DETECTION_DICTIONARY}
        onCorrect={jest.fn()}
      />,
    );

    expect(screen.getByTestId("correction-detected-1")).toHaveTextContent("Piso 4");
  });

  test("dice cuáles quedaron sin ubicar, en vez de esconderlas", () => {
    render(
      <LocationCorrectionPanel
        tasks={[task({ id: 2, name: "Instalación de redes secas" })]}
        dictionary={EMPTY_DETECTION_DICTIONARY}
        onCorrect={jest.fn()}
      />,
    );

    expect(screen.getByTestId("correction-detected-2")).toHaveTextContent("Obra general");
  });

  test("corregir entrega el nombre de la tarea, el valor y el motivo", async () => {
    const user = userEvent.setup();
    const onCorrect = jest.fn();
    render(
      <LocationCorrectionPanel
        tasks={[task({ id: 2, name: "Instalación de redes secas" })]}
        dictionary={EMPTY_DETECTION_DICTIONARY}
        onCorrect={onCorrect}
      />,
    );

    await user.type(screen.getByLabelText("Nivel corregido de Instalación de redes secas"), "4");
    await user.type(screen.getByLabelText("Motivo de la corrección de Instalación de redes secas"), "Va en el piso 4");
    await user.click(screen.getByRole("button", { name: "Corregir Instalación de redes secas" }));

    expect(onCorrect).toHaveBeenCalledWith({
      taskName: "Instalación de redes secas",
      value: "4",
      note: "Va en el piso 4",
    });
  });

  test("sin motivo no se guarda: en seis meses nadie sabría si sigue haciendo falta", async () => {
    const user = userEvent.setup();
    const onCorrect = jest.fn();
    render(
      <LocationCorrectionPanel
        tasks={[task({ id: 2, name: "Instalación de redes secas" })]}
        dictionary={EMPTY_DETECTION_DICTIONARY}
        onCorrect={onCorrect}
      />,
    );

    await user.type(screen.getByLabelText("Nivel corregido de Instalación de redes secas"), "4");
    await user.click(screen.getByRole("button", { name: "Corregir Instalación de redes secas" }));

    expect(onCorrect).not.toHaveBeenCalled();
    expect(screen.getByRole("alert")).toHaveTextContent("Escribe por qué la corriges");
  });

  test("una tarea ya corregida se marca como corregida a mano", () => {
    render(
      <LocationCorrectionPanel
        tasks={[task({ id: 2, name: "Instalación de redes secas" })]}
        dictionary={{
          corrections: [
            {
              kind: "ubicacion",
              key: "instalacion de redes secas",
              value: "4",
              note: "Va en el piso 4",
              recordedAt: "2026-08-08T10:00:00.000Z",
            },
          ],
        }}
        onCorrect={jest.fn()}
      />,
    );

    expect(screen.getByTestId("correction-source-2")).toHaveTextContent("Corregida a mano");
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/lob/LocationCorrectionPanel.test.tsx`
Esperado: FALLA con `Cannot find module './LocationCorrectionPanel' from 'src/components/lob/LocationCorrectionPanel.test.tsx'`

- [ ] **Paso 3: Implementación mínima**

```tsx
"use client";

import { useState } from "react";
import type { GanttTask } from "@/components/gantt/types";
import type { DetectionDictionary } from "@/lib/scheduling/detection/dictionary";
import { formatLocationLabel } from "@/lib/scheduling/detection/location";
import { resolveTaskLocation } from "@/lib/scheduling/detection/taskLocation";

/**
 * Donde se cierra el ciclo de aprendizaje que P3 construyó a medias.
 *
 * El motor exponía el diccionario y su API, pero la pantalla que lo usaría
 * quedó en otro proyecto: el motor aprendía de correcciones que nadie podía
 * escribir. La corrección es **puntual** —se guarda contra el nombre exacto
 * de esa tarea— y no una regla que arrastre a las de nombre parecido.
 */
export interface LocationCorrectionPanelProps {
  tasks: GanttTask[];
  dictionary: DetectionDictionary;
  onCorrect: (input: { taskName: string; value: string; note: string }) => void;
}

export default function LocationCorrectionPanel({
  tasks,
  dictionary,
  onCorrect,
}: LocationCorrectionPanelProps) {
  const [values, setValues] = useState<Record<string, string>>({});
  const [notes, setNotes] = useState<Record<string, string>>({});
  const [error, setError] = useState<string | null>(null);

  const submit = (task: GanttTask) => {
    const key = String(task.id);
    const value = (values[key] ?? "").trim();
    const note = (notes[key] ?? "").trim();

    if (!value) {
      setError("Escribe el nivel al que va la tarea.");
      return;
    }
    if (!note) {
      setError("Escribe por qué la corriges: sin motivo, en seis meses nadie sabrá si sigue haciendo falta.");
      return;
    }

    setError(null);
    onCorrect({ taskName: task.name, value, note });
  };

  return (
    <section data-testid="location-correction-panel" className="apple-section p-4">
      <h3 className="text-sm font-semibold text-[var(--color-text-strong)]">
        Corregir ubicaciones
      </h3>
      <p className="mt-1 max-w-prose text-xs text-[var(--color-text-muted)]">
        Lo que corrijas aquí manda sobre la detección automática la próxima vez.
        Es puntual: solo afecta a esta tarea, no a las de nombre parecido.
      </p>

      {error && (
        <p role="alert" className="mt-2 text-xs font-semibold text-[var(--aia-warn-main)]">
          {error}
        </p>
      )}

      <ul className="mt-3 divide-y divide-[var(--color-hairline)]">
        {tasks.map((task) => {
          const key = String(task.id);
          const { location, scope } = resolveTaskLocation(task, tasks, dictionary);

          return (
            <li key={key} className="grid gap-2 py-3 md:grid-cols-[minmax(0,1fr)_120px_140px_minmax(0,1fr)_auto]">
              <span className="truncate text-sm text-[var(--color-text-strong)]">{task.name}</span>

              <span
                data-testid={`correction-detected-${key}`}
                className="text-sm text-[var(--color-text-muted)]"
              >
                {location ? formatLocationLabel(location) : "Obra general"}
              </span>

              <span
                data-testid={`correction-source-${key}`}
                className="text-xs text-[var(--color-text-muted)]"
              >
                {scope === "diccionario" ? "Corregida a mano" : "Detección automática"}
              </span>

              <div className="grid gap-2">
                <input
                  aria-label={`Nivel corregido de ${task.name}`}
                  value={values[key] ?? ""}
                  onChange={(event) =>
                    setValues((current) => ({ ...current, [key]: event.target.value }))
                  }
                  className="rounded border border-[var(--color-hairline)] px-2 py-1 text-sm"
                />
                <input
                  aria-label={`Motivo de la corrección de ${task.name}`}
                  value={notes[key] ?? ""}
                  onChange={(event) =>
                    setNotes((current) => ({ ...current, [key]: event.target.value }))
                  }
                  className="rounded border border-[var(--color-hairline)] px-2 py-1 text-sm"
                />
              </div>

              <button
                type="button"
                aria-label={`Corregir ${task.name}`}
                onClick={() => submit(task)}
                className="self-start rounded bg-[var(--aia-corp-main)] px-3 py-1 text-sm font-semibold text-[var(--color-text-on-primary)]"
              >
                Corregir
              </button>
            </li>
          );
        })}
      </ul>
    </section>
  );
}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/lob/LocationCorrectionPanel.test.tsx`
Esperado: PASA — 5 tests en verde.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/lob/LocationCorrectionPanel.tsx v2/src/components/lob/LocationCorrectionPanel.test.tsx && git commit -m "feat(remates): ya hay donde corregir la ubicacion que el motor asigno"
```

---

## Tarea 18: El panel entra en la Línea de Balance

**Archivos:**
- Modificar: `src/components/views/GanttView.tsx:537-540` (`automaticLOB`), `:1152` (el payload de guardado), `:2164-2174` (el bloque `activeView === "lob"`)
- Test: `src/components/views/GanttView.test.tsx`

**Interfaces:**
- Consume: `LocationCorrectionPanel` (props de la tarea 17); `rememberCorrection(dictionary, input)` de `src/lib/scheduling/detection/dictionary.ts`; `generateAutomaticLOBFromTasks(tasks, matrixPlan, dictionary)` de la tarea 16; `ProjectData.detectionDictionary` de la tarea 15.
- Produce: nada exportado; es cableado.

- [ ] **Paso 1: Escribir el test que falla**

Se añade a `src/components/views/GanttView.test.tsx`:

```tsx
describe("GanttView · corregir ubicaciones desde la Línea de Balance (R4)", () => {
  test("la Línea de Balance ofrece el panel de correcciones", async () => {
    const user = userEvent.setup();
    renderGanttView();

    await user.click(screen.getByTestId("sidebar-view-lob"));

    expect(screen.getByTestId("location-correction-panel")).toBeInTheDocument();
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/views/GanttView.test.tsx -t "ofrece el panel de correcciones" --runInBand`
Esperado: FALLA con `Unable to find an element by: [data-testid="location-correction-panel"]`

- [ ] **Paso 3: Implementación mínima**

En `src/components/views/GanttView.tsx`, los imports nuevos:

```tsx
import LocationCorrectionPanel from "@/components/lob/LocationCorrectionPanel";
import {
  EMPTY_DETECTION_DICTIONARY,
  rememberCorrection,
  type DetectionDictionary,
} from "@/lib/scheduling/detection/dictionary";
```

El estado y el manejador, junto al resto de estado del proyecto:

```tsx
  const [detectionDictionary, setDetectionDictionary] = useState<DetectionDictionary>(
    initialDetectionDictionary ?? EMPTY_DETECTION_DICTIONARY,
  );

  const handleCorrectLocation = useCallback(
    (input: { taskName: string; value: string; note: string }) => {
      setDetectionDictionary((current) =>
        rememberCorrection(current, {
          kind: "ubicacion",
          name: input.taskName,
          value: input.value,
          note: input.note,
          recordedAt: new Date().toISOString(),
        }),
      );
    },
    [],
  );
```

> `initialDetectionDictionary` es una prop nueva de `GanttViewProps` (`detectionDictionary?: DetectionDictionary`), que se pasa desde el componente contenedor igual que ya se pasa `initialMatrixPlan` (línea 2310). Si no se pasa, el diccionario arranca vacío.

`automaticLOB` consulta el diccionario:

```tsx
  const automaticLOB = useMemo(
    () => generateAutomaticLOBFromTasks(calculatedTasks, syncedMatrixPlan, detectionDictionary),
    [calculatedTasks, detectionDictionary, syncedMatrixPlan],
  );
```

El payload de guardado, junto a `matrixPlan: syncedMatrixPlan,`:

```tsx
        detectionDictionary,
```

Y el bloque de la vista:

```tsx
          {activeView === "lob" && (
            <div className="flex min-h-0 min-w-0 flex-1 flex-col gap-3 overflow-auto">
              <div className="min-h-0 min-w-0 flex-1">
                <LineOfBalance
                  activities={automaticLOB.activities}
                  units={automaticLOB.units}
                  scale={scale}
                  onScaleChange={setScale}
                />
              </div>
              <LocationCorrectionPanel
                tasks={calculatedTasks}
                dictionary={detectionDictionary}
                onCorrect={handleCorrectLocation}
              />
            </div>
          )}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/views/GanttView.test.tsx --runInBand`
Esperado: PASA — la suite completa.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/views/GanttView.tsx v2/src/components/views/GanttView.test.tsx && git commit -m "feat(remates): el ciclo de aprendizaje del motor se cierra en la linea de balance"
```

---

# BLOQUE R5 — El módulo conserva su tramo de ejes

## Tarea 19: `Módulo 1.1 (Ejes A-D)` resuelve con módulo **y** tramo

**Archivos:**
- Modificar: `src/lib/scheduling/detection/location.ts:255-274` (`extractLocation`)
- Test: `src/lib/scheduling/detection/location.test.ts`

**Interfaces:**
- Consume: `LOCATION_PATTERNS`, `LocationSpan`, `LocationMatch` — todos ya existentes en el mismo archivo.
- Produce: `extractLocation(text: string): LocationMatch | null` — la firma no cambia; cambia que el resultado puede traer `span` aunque el patrón ganador no sea el de eje.

- [ ] **Paso 1: Escribir el test que falla**

Se añade a `src/lib/scheduling/detection/location.test.ts`:

```ts
describe("El módulo conserva su tramo de ejes (R5)", () => {
  test("«Módulo 1.1 (Ejes A-D)» resuelve como módulo, con el tramo puesto", () => {
    const result = extractLocation("Estructura Módulo 1.1 (Ejes A-D)");

    expect(result?.label).toBe("Módulo");
    expect(result?.value).toBe(1.1);
    expect(result?.span).toEqual({
      rawFrom: "A",
      rawTo: "D",
      from: 1,
      to: 4,
    });
  });

  test("el módulo sigue ganando al eje: la precedencia no cambia", () => {
    expect(extractLocation("Módulo 2 Ejes B-C")?.label).toBe("Módulo");
  });

  test("un módulo sin ejes resuelve igual que hoy, sin tramo", () => {
    const result = extractLocation("Acabados Módulo 3");

    expect(result?.label).toBe("Módulo");
    expect(result?.value).toBe(3);
    expect(result?.span).toBeUndefined();
  });

  test("el tramo de un eje solo, sin módulo, no cambia", () => {
    const result = extractLocation("Cimentación Ejes A-D");

    expect(result?.label).toBe("Eje");
    expect(result?.span?.to).toBe(4);
  });

  test("el edificio también conserva el tramo de ejes que lleve detrás", () => {
    const result = extractLocation("Edificio 2 (Ejes J-DB08)");

    expect(result?.label).toBe("Edificio");
    expect(result?.span?.crossesGrids).toBe(true);
  });

  test("un tramo de piso no se contamina con ejes de otra tarea", () => {
    const result = extractLocation("Escalera Piso 1 a 2");

    expect(result?.span?.rawFrom).toBe("1");
    expect(result?.span?.rawTo).toBe("2");
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/scheduling/detection/location.test.ts -t "resuelve como módulo, con el tramo puesto"`
Esperado: FALLA con `expect(received).toEqual(expected)` — `received: undefined`, porque el patrón `Módulo` no declara `spanOf` y `extractLocation` devuelve al primer acierto sin mirar más.

- [ ] **Paso 3: Implementación mínima**

En `src/lib/scheduling/detection/location.ts`, una constante nueva justo después de `LOCATION_PATTERNS` (que ya está definido) y antes de `formatLocationLabel`:

```ts
/**
 * El patrón de eje con principio y fin, aparte del recorrido de prioridad.
 *
 * La precedencia no cambia: el módulo sigue ganando al eje porque es la unidad
 * de producción (D3). Lo que cambia es que deja de **descartar** el tramo. Es
 * dato adicional, no un cambio de criterio: `label` y `value` siguen siendo
 * los del módulo, y todo lo que ordena por `value` no se entera.
 */
const AXIS_SPAN_PATTERN = LOCATION_PATTERNS.find(
  (pattern) => pattern.label === "Eje" && pattern.spanOf != null,
);
```

Y `extractLocation` gana el rescate del tramo:

```ts
export function extractLocation(text: string): LocationMatch | null {
  const normalized = normalizeName(text);
  for (const pattern of LOCATION_PATTERNS) {
    const match = normalized.match(pattern.regex);
    if (!match) continue;
    const value = pattern.valueOf(match);
    if (!Number.isFinite(value)) continue;

    const span = pattern.spanOf?.(match) ?? undefined;
    const result: LocationMatch = {
      label: pattern.label,
      raw: match[1] ?? match[0],
      value,
    };
    if (span) {
      result.span = span;
      return result;
    }

    // El patrón ganador no es de tramo, pero el nombre puede llevar uno
    // detrás: «Módulo 1.1 (Ejes A-D)» es el módulo 1.1 **y** el tramo A-D.
    if (pattern.label !== "Eje" && AXIS_SPAN_PATTERN) {
      const axisMatch = normalized.match(AXIS_SPAN_PATTERN.regex);
      const axisSpan = axisMatch ? AXIS_SPAN_PATTERN.spanOf?.(axisMatch) : null;
      if (axisSpan) result.span = axisSpan;
    }

    return result;
  }
  return null;
}
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/scheduling/detection --runInBand`
Esperado: PASA — todas las suites del motor de detección, incluidas las de la Estación 16 y las de obra lineal.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/lib/scheduling/detection/location.ts v2/src/lib/scheduling/detection/location.test.ts && git commit -m "feat(remates): el modulo gana al eje pero ya no le tira el tramo"
```

---

# BLOQUE R6 — Los pendientes que esperan un dato

Cuatro pendientes bloqueados por un dato que hoy no existe. Solo uno tiene código: el renombrado. Los otros tres entran como registro escrito y, en el caso de las abscisas, como caso de prueba **en espera**.

## Tarea 20: «Productividad» no vuelve a aparecer en la interfaz

**Archivos:**
- Crear: `src/components/views/copyProductividad.test.ts`
- Modificar: `src/components/views/TypicalUnitView.tsx:35-38` (el comentario)
- Modificar: `src/components/views/TypicalUnitView.test.tsx:79`

**Interfaces:**
- Consume: `node:fs`, `node:path`.
- Produce: nada; es un test guardián.

- [ ] **Paso 1: Escribir el test que falla**

```ts
import { readdirSync, readFileSync, statSync } from "node:fs";
import path from "node:path";

/**
 * «Productividad» prometía obra ejecutada por día, y el número era el inverso
 * de la duración. Se llama **Ritmo** en toda la interfaz, sin excepción.
 *
 * La palabra se prohíbe entera —comentarios incluidos— porque un comentario
 * que la nombra es la semilla de que vuelva a la pantalla en el siguiente
 * copiar-pegar. La productividad real llegará cuando la matriz aporte
 * cantidades de obra ejecutada; ese es el dato que falta, y hasta entonces
 * este nombre está reservado.
 */
const SRC_DIR = path.resolve(__dirname, "../..");
const ESTE_ARCHIVO = "copyProductividad.test.ts";

function archivosDeInterfaz(dir: string): string[] {
  return readdirSync(dir).flatMap((entry) => {
    const full = path.join(dir, entry);
    if (statSync(full).isDirectory()) return archivosDeInterfaz(full);
    if (entry === ESTE_ARCHIVO) return [];
    return /\.(ts|tsx)$/.test(entry) ? [full] : [];
  });
}

describe("Detector de «Productividad» en la interfaz (R6)", () => {
  test("la palabra no aparece en ningún archivo de src", () => {
    const culpables = archivosDeInterfaz(SRC_DIR)
      .filter((file) => /Productividad/i.test(readFileSync(file, "utf8")))
      .map((file) => path.relative(SRC_DIR, file));

    expect(culpables).toEqual([]);
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/components/views/copyProductividad.test.ts`
Esperado: FALLA con `culpables: ["components/views/TypicalUnitView.tsx", "components/views/TypicalUnitView.test.tsx"]` frente al `[]` esperado.

- [ ] **Paso 3: Implementación mínima**

En `src/components/views/TypicalUnitView.tsx:35-38`, el comentario se reescribe sin nombrar la palabra prohibida:

```tsx
          {/*
            El rótulo anterior prometía algo que el número no era: es el
            inverso de la duración, y no hay ninguna cantidad de obra detrás.
            Se llama por su nombre y se explica de dónde sale (M2).
          */}
```

Y el texto visible del párrafo, en la misma vista, deja de prometer lo que falta nombrándolo:

```tsx
            El <strong>ritmo (1/día)</strong> es el inverso de la duración:
            cuántos niveles por día da el paso actual. Cuando la matriz aporte
            cantidades de obra ejecutada, este número podrá medir rendimiento
            real.
```

En `src/components/views/TypicalUnitView.test.tsx:79`, el aserto pasa a describir lo mismo sin escribir la palabra:

```tsx
    expect(screen.queryByText(/^Product[a-zá-ú]+dad$/i)).not.toBeInTheDocument();
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/components/views/copyProductividad.test.ts src/components/views/TypicalUnitView.test.tsx --runInBand`
Esperado: PASA — las dos suites en verde.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/components/views/copyProductividad.test.ts v2/src/components/views/TypicalUnitView.tsx v2/src/components/views/TypicalUnitView.test.tsx && git commit -m "test(remates): la palabra productividad queda reservada hasta que exista el dato"
```

---

## Tarea 21: El caso de prueba de las abscisas, escrito y en espera

**Archivos:**
- Modificar: `src/lib/scheduling/detection/location.test.ts` (se añade un `describe` con `test.skip`)

**Interfaces:**
- Consume: `extractLocation(text: string): LocationMatch | null` de `src/lib/scheduling/detection/location.ts`.
- Produce: nada; el test queda desactivado a propósito y **no** se implementa el patrón.

- [ ] **Paso 1: Escribir el test que falla**

```ts
/**
 * Abscisas de obra lineal: `K12+340` es el kilómetro 12 más 340 metros.
 *
 * **En espera de un dato, no de trabajo.** El patrón no se implementa porque
 * no hay ningún `.mpp` real de túnel o de vía sobre el que verificarlo, y
 * escribirlo a ciegas produciría un extractor que acierta con el ejemplo
 * inventado y falla con la nomenclatura real — que es exactamente lo que pasó
 * con el vocabulario vertical antes de tener la Estación 16.
 *
 * **Disparador:** en cuanto llegue un `.mpp` de túnel o vía, se sustituyen
 * estos nombres por los reales del archivo, se quita el `.skip` y se
 * implementa el patrón hasta que pase.
 */
describe("Abscisas de obra lineal — en espera de un .mpp real (R6)", () => {
  test.skip("«Excavación K12+340» ubica en el kilómetro 12 más 340 metros", () => {
    const result = extractLocation("Excavación K12+340");

    expect(result?.label).toBe("Abscisa");
    expect(result?.value).toBe(12340);
  });

  test.skip("«Revestimiento K12+340 a K12+560» resuelve como tramo", () => {
    const result = extractLocation("Revestimiento K12+340 a K12+560");

    expect(result?.label).toBe("Abscisa");
    expect(result?.span).toEqual({
      rawFrom: "K12+340",
      rawTo: "K12+560",
      from: 12340,
      to: 12560,
    });
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/scheduling/detection/location.test.ts -t "Abscisas de obra lineal"`
Esperado: no falla ni pasa — la salida dice `2 skipped`. Ese es el resultado buscado: el caso está escrito y desactivado. Para comprobar que el test es real y no una cáscara, quitar el `.skip` del primero y volver a correr: FALLA con `expect(received).toBe("Abscisa") // Received: undefined`. Restaurar el `.skip` acto seguido.

- [ ] **Paso 3: Implementación mínima**

**No hay implementación, y eso es el entregable.** Se restaura el `.skip` y se deja el bloque tal cual, con su comentario de disparador. Escribir el patrón de abscisas ahora sería trabajo imposible de verificar el primer día.

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/scheduling/detection/location.test.ts --runInBand`
Esperado: PASA — la suite en verde con `2 skipped` en el resumen.

- [ ] **Paso 5: Commit**

```bash
git add v2/src/lib/scheduling/detection/location.test.ts && git commit -m "test(remates): el caso de las abscisas queda escrito, esperando un mpp real"
```

---

## Tarea 22: Los cuatro pendientes de R6 y las cuatro decisiones de R7, registrados

**Archivos:**
- Modificar: `docs/EXPERIMENTS.md` (dos secciones nuevas al final)
- Test: `src/lib/docs/docsConsistency.test.ts` (se le añade un `describe`)

**Interfaces:**
- Consume: `readDoc("EXPERIMENTS.md")` de `src/lib/docs/docsConsistency.ts`.
- Produce: nada nuevo.

- [ ] **Paso 1: Escribir el test que falla**

```ts
describe("R6 y R7 quedan registrados donde nadie los confunda con trabajo olvidado", () => {
  const experiments = readDoc("EXPERIMENTS.md");

  test("los cuatro pendientes que esperan un dato están escritos con su disparador", () => {
    expect(experiments).toContain("## Pendientes que esperan un dato (R6)");
    for (const pendiente of [
      "Ritmo, no productividad",
      "Abscisas `K12+340`",
      "RUM en obra",
      "Presupuesto desde PDC",
    ]) {
      expect(experiments).toContain(pendiente);
    }
  });

  test("las cuatro decisiones diferidas están escritas con el caso que las obliga", () => {
    expect(experiments).toContain("## Decisiones diferidas (R7)");
    for (const decision of [
      "Cómo dibujar un tramo en la Línea de Balance",
      "El orden entre familias de eje",
      "Deshacer granular más allá de R1",
      "«Fin cambia la duración»",
    ]) {
      expect(experiments).toContain(decision);
    }
  });
});
```

- [ ] **Paso 2: Correr el test y verlo fallar**

Comando: `cd v2 && npx jest src/lib/docs/docsConsistency.test.ts -t "esperan un dato están escritos"`
Esperado: FALLA con `expect(received).toContain("## Pendientes que esperan un dato (R6)")`.

- [ ] **Paso 3: Implementación mínima**

Se añaden al final de `docs/EXPERIMENTS.md`:

```markdown
## Pendientes que esperan un dato (R6)

No están bloqueados por falta de trabajo, sino por falta de un dato que hoy no existe. Subirlos como tareas
normales produciría trabajo imposible el primer día. Tienen dueño: el dato que les falta.

| Pendiente | Qué falta | Qué ya se hizo | Disparador |
|---|---|---|---|
| Ritmo, no productividad (P3+P4) | Cantidad de obra ejecutada por actividad | El indicador se llama **Ritmo (1/día)** en toda la interfaz, y un test prohíbe el nombre anterior en todo `v2/src` (`copyProductividad.test.ts`) | Que la matriz aporte cantidades de obra ejecutada, no solo previstas |
| Abscisas `K12+340` (P3b) | Un `.mpp` real de túnel o de vía | El caso de prueba está escrito y desactivado en `location.test.ts`, con los dos escenarios: punto y tramo | Llega un `.mpp` de túnel o vía: se sustituyen los nombres por los reales, se quita el `.skip` y se implementa hasta que pase |
| RUM en obra (E47) | Usuarios reales usando la app en campo | Nada de código, a propósito: es una **medición pendiente**, no deuda técnica | Que haya obra usando el visor a diario con red de campo |
| Presupuesto desde PDC | Es integración entre dos aplicaciones, no una función del visor | **Fuera de alcance del goal maestro.** El punto de entrada es la carga manual de partidas, que se deja funcionando y sin pulir | Que exista una decisión de producto sobre integrar PDC y el visor |

## Decisiones diferidas (R7)

Cuatro decisiones tomadas con motivo, no trabajo sin hacer. Se resuelven **cuando aparezca el caso que las
obliga**, y hasta entonces se quedan escritas aquí para que ninguna auditoría futura las cuente como deuda.

- **Cómo dibujar un tramo en la Línea de Balance.** Hoy cada unidad es una fila y un tramo ocupa varias. El
  motor ya tiene el dato tras R5 (`LocationMatch.span`); la vista decidirá cuando se aborde la Línea de
  Balance de obra lineal. *Caso que la obliga:* un cronograma de obra lineal con tramos en la Línea de Balance.
- **El orden entre familias de eje** (letras, números, series como `DB`). Se resolvió por familia y luego por
  índice — «lo único defendible» sin conocer la geometría real del proyecto. Queda como **supuesto declarado,
  no verificado**. *Caso que la obliga:* un plano de ejes real que contradiga el orden supuesto.
- **Deshacer granular más allá de R1.** R1 cubre las operaciones del borrador del editor. El deshacer paso a
  paso dentro de la matriz **ya construida y persistida** sigue siendo proyecto propio. *Caso que la obliga:*
  que alguien pierda trabajo en la matriz aplicada, no en el borrador.
- **«Fin cambia la duración».** Si el motor no lo soporta limpiamente, el campo queda en **solo lectura** y se
  informa. Es un límite aceptado con salida honesta, no una implementación a medias. *Caso que la obliga:* que
  el motor de cálculo pase a soportar el modo de tarea que lo hace posible.
```

- [ ] **Paso 4: Correr el test y verlo pasar**

Comando: `cd v2 && npx jest src/lib/docs --runInBand`
Esperado: PASA — la suite de `docs/` completa, con los cuatro `describe` acumulados de R3, R6 y R7.

- [ ] **Paso 5: Commit**

```bash
git add docs/EXPERIMENTS.md v2/src/lib/docs/docsConsistency.test.ts && git commit -m "docs(remates): los ocho pendientes que no son trabajo quedan con dueno escrito"
```

---

# Verificación final del proyecto

- [ ] **Suite completa en serie**

Comando: `cd v2 && npx jest --runInBand`
Esperado: PASA — al menos las **1.400** de partida más las nuevas de este proyecto, sin ninguna en rojo y con los 2 `skipped` de las abscisas.

- [ ] **Lint y tipos**

```bash
cd v2 && npx eslint src/lib/gantt/viewSidebarBlurb.ts src/lib/matrix/draftHistory.ts src/lib/matrix/useDraftHistory.ts src/lib/gantt/columnLabel.ts src/lib/docs/docsConsistency.ts src/components/lob/LocationCorrectionPanel.tsx src/components/gantt/toolbar/ViewSidebar.tsx src/components/gantt/table/ColumnHeader.tsx src/components/gantt/table/GanttTable.tsx src/components/views/MatrixEditorView.tsx src/components/views/GanttView.tsx src/lib/scheduling/detection/location.ts src/lib/scheduling/lob.ts src/app/actions/project.ts
```
Esperado: sin errores.

```bash
cd v2 && npx tsc --noEmit 2>&1 | grep -vE "\.test\.|e2e/"
```
Esperado: salida **vacía**.

- [ ] **Build**

Comando: `cd v2 && npx next build`
Esperado: build correcto.

- [ ] **Comprobación en navegador**

Levantar el entorno y abrir un proyecto con matriz. Recorrido mínimo, uno por bloque:

1. **R0** — el menú lateral muestra bajo «Matriz» el número real de ubicaciones y bajo «Recursos» el de recursos; en un proyecto sin matriz, la entrada explica para qué sirve crearla.
2. **R1** — en el editor de matriz, borrar un alcance con celdas dentro y pulsar `⌘Z`: vuelven el alcance y sus celdas. `⌘⇧Z` lo borra otra vez. El diálogo ya no dice que sea irreversible.
3. **R2** — estrechar la columna «Predecesoras» arrastrando su borde: el encabezado pasa a «Pred.» en vez de partirse, y el `title` sigue diciendo «Predecesoras».
4. **R4** — en la Línea de Balance, corregir la ubicación de una tarea que salió como «Obra general», guardar, recargar la página y comprobar que la corrección sigue puesta y que la tarea aparece en su nivel.
5. **R5** — con un cronograma que tenga tareas de módulo con paréntesis de ejes, comprobar que se agrupan por módulo y no perdieron el tramo.

---

## Orden de ejecución y dependencias

R0 (tareas 1-4) → R1 (5-8) → R2 (9-11) → R3 (12-14) → R4 (15-18) → R5 (19) → R6 (20-22).

Dentro de cada bloque el orden es estricto. Entre bloques, las únicas dependencias reales son:

- La tarea 3 necesita la 1 y la 2.
- Las tareas 7 y 8 necesitan la 5 y la 6.
- La tarea 11 necesita la 9 y la 10.
- Las tareas 13, 14 y 22 necesitan el módulo `docsConsistency.ts` de la tarea 12.
- La tarea 18 necesita la 15, la 16 y la 17.

R5 (tarea 19) y R6 (tareas 20-22) son independientes del resto y se pueden adelantar si conviene.
